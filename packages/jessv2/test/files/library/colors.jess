// @import { config as baseConfig } from './config.jess';
// @let config: $baseConfig;
// @let colors: $config.colors;

@use './config.jess' (config);
@let colors: $config.colors;


.rule {
  color: $colors.primary;
}