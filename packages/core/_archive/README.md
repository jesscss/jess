# Archived docs (`@jesscss/core`)

This folder holds **dated snapshots** of one-off analyses and debugging notes that used to live at the package root.

Prefer keeping current guidance in:
- `packages/core/README.md`
- scoped Cursor rules under `.cursor/rules/`

