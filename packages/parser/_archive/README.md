# Archived docs (`packages/parser`)

This folder holds **dated snapshots** of parser debugging/analysis notes that are useful as historical context but are not intended to be “current spec”.

If you’re investigating a parser issue, start with the package code and then consult these snapshots (or git history) for prior hypotheses.

