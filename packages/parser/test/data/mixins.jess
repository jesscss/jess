@mixin domeMixin {
  one: 1;
  two: 2;
}
@mixin domeMixin () {
  one: 1;
  two: 2;
}
@mixin domeMixin (one, two) when ($two = 2) {}
@mixin domeMixin (one, two: 2) {}
@mixin domeMixin (one: 1, two: 2) {}

$domeMixin();
$domeMixin(1);
$domeMixin(1, 2);

$iif(!one_1_cond && !one_2_cond, def());