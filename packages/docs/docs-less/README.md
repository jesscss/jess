# Less Docs Site

Docusaurus renderer for the Less-facing docs site.

This package renders content from the canonical docs source in:

- `packages/docs-content/docs/less`

## Commands

- `pnpm --filter @jesscss/docs-less dev`
- `pnpm --filter @jesscss/docs-less build`
