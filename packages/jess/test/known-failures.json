{
  "note": [
    "NAMED baseline of known-failing tests in the `jess` package suite, consumed by",
    "`node scripts/vitest-ratchet.mjs --package packages/jess --baseline packages/jess/test/known-failures.json`",
    "(run it with `pnpm run verify:jess-suite-ratchet`).",
    "",
    "It is a SET, never a count. A count cannot tell 'nothing changed' apart from",
    "'you fixed one and broke another' — both read as the same number. Each entry names one test,",
    "so the failure output and the git diff both say which test moved and which way.",
    "",
    "Shrinking is easy: delete the entry. The gate REQUIRES you to, and names the",
    "entry to delete, the moment a listed test starts passing. Growing is deliberate:",
    "a new failure fails the gate and must be fixed, not appended here.",
    "",
    "`flaky: true` parks a genuinely unstable test: it never gates in either",
    "direction, and it MUST carry a `tracking` pointer. It is an open bug, not an",
    "excuse — do not use it to silence a deterministic failure."
  ],
  "knownFailures": [
    {
      "test": "test/less/bootstrap-clean-repro.test.ts > bootstrap clean render renders and collects rejections",
      "reason": "Bootstrap 4.6 render is missing the `--breakpoint-*` custom properties the test asserts on."
    }
  ],
  "knownFlakyElsewhere": [
    {
      "test": "tests-unit/extend-exact/extend-exact.less",
      "lane": "pnpm run test:less:test-data (packages/jess/test/less/all-less.test.ts, vitest.less-test-data.config.ts)",
      "status": "NOT allowlisted anywhere — that lane still gates on it at full strength.",
      "reason": "Intermittently fails only in full-suite runs. Under separate investigation as real cross-compile state contamination, NOT test flakiness. Recorded here so this ratchet work cannot be mistaken for having buried it."
    }
  ]
}
