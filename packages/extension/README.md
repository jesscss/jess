# `@jesscss/extension`

VS Code / Cursor extension that launches the Jess language server from `@jesscss/language-service`.

## Notes

- During development, you will typically disable the built-in **CSS Language Features** extension to avoid duplicated/mixed results.
- The extension should register for: `css`, `less`, `scss` (and later `jess`).

