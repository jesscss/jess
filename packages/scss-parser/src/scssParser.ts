/**
 * @deprecated Kept for compatibility with older experimental imports.
 *
 * The maintained SCSS parser entrypoint is `ScssActionsParser` in
 * `./scssActionsParser.ts` and the public wrapper `Parser` in `./index.ts`.
 */
export const DEPRECATED_SASS_PARSER_NOTICE =
  'Deprecated: use ScssActionsParser/Parser from @jesscss/scss-parser.';