/**
 * @deprecated Legacy/placeholder package.
 *
 * The SCSS/Sass parsing story in this repo is not currently maintained.
 * This package exists mainly so older experiments/packages can depend on it
 * without breaking installs.
 *
 * Prefer:
 * - `jess` (Compiler) for compilation
 * - `@jesscss/css-parser` / `@jesscss/less-parser` for parsing work
 */
export const LEGACY_NOTICE =
  'Legacy package: SCSS parser is not currently maintained in this repo.';