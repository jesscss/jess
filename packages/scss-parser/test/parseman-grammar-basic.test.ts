/**
 * Smoke tests for the Parséman-based SCSS grammar.
 * TODO: migrate to a functional grammar-fn.ts entry point once ScssGrammar
 * is ported — the class grammar is now builder-only (no parse() method).
 */
import { describe, it } from 'vitest';

describe.skip('ScssGrammar (Parséman) — basic smoke tests', () => {
  it('placeholder — re-enable once ScssGrammar has a functional grammar-fn', () => {});
});
