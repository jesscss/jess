---
title: Migrating to Jess
sidebar_label: Migrating to Jess
description: Move a Less or Sass codebase onto Jess with jess convert and side-by-side syntax mapping tables.
audiences:
  - jess
  - sass
origin: jess
slug: /guides/migrating-to-jess
---
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import AudienceGate from '@theme/AudienceGate';

<AudienceGate include={['jess']}>

Moving an existing **Less** or **Sass** codebase onto **Jess**? This guide pairs
the `jess convert` first pass with a side-by-side mapping so you can see exactly
what each construct becomes in Jess syntax.

</AudienceGate>

<AudienceGate include={['sass']}>

Coming from **Sass/SCSS**? Jess parses your SCSS today (it calls the supported
subset **Sass+**), and `jess convert` rewrites it to native `.jess`. The tables
below read left-to-right: **your Sass** on the left, **Jess** on the right.

</AudienceGate>

:::note
`jess convert` is available today and is best used as a first-pass migration assistant. Plan on reviewing converted output and iterating on edge cases (especially complex mixin/namespace patterns) as Jess syntax continues to evolve in alpha.
:::

## Migrating Less to Jess

```sh
jess convert "./src/less/**/*.less" "./src/styles"
```

## Conversion steps

Jess starts by parsing your Less/Sass source (or other plugin-provided language), then evaluates the tree so it can rewrite syntax with real context (imports, function calls, variable lookups). Think of it as "mechanical migration first, human polish second."

### At-rules

Jess will convert at-rules to their prefixed counterparts. In import rules, Jess will distinguish between file-relative and module-relative imports, as Jess requires file-relative imports to be explicit.

E.g. `@import` will become `@-import`

<Tabs>
  <TabItem value="Less">
  ```less
  @import "variables"; // variables.less
  ```
  </TabItem>
  <TabItem value="Sass">
  ```less
  @import "variables"; // variables.scss
  ```
  </TabItem>
  <TabItem value="Jess">
  ```less
  @-import "./variables"; // variables.jess
  ```
  </TabItem>
</Tabs>

:::tip Prefer `@-compose` once converted
`@-import` is a **legacy bridge** — it works and eases a mechanical port, but it
emits a deprecation warning because it re-exports everything and changes the
evaluation context per call. The Jess module system is
[`@-compose`](/docs/Language/atrules) (namespaced, isolated scope, `with { }`
configuration). A Sass `@use "./theme";` maps most cleanly to
`@-compose "./theme";`, not `@-import`. Convert with `@-import` for the first
pass, then promote real modules to `@-compose`.
:::

### Variable declarations

Less-style variable declarations will be converted to Jess-style variable declarations, which resemble Sass-style variable declarations.

<Tabs>
  <TabItem value="Less">
  ```less
  @color: #06c;
  ```
  </TabItem>
  <TabItem value="Sass">
  ```less
  $color: #06c;
  ```
  </TabItem>
  <TabItem value="Jess">
  ```less
  $color: #06c;
  ```
  </TabItem>
</Tabs>

### Variable references

#### Less-to-Jess

<div style={{ display: 'flex', flexDirection: 'row', gap: '1rem' }}>
  <div style={{ flex: 1 }}>
  ```less
  value: @color;
  ```
  </div>
  <div style={{ flex: 1 }}>
  ```less
  value: $color;
  ```
  </div>
</div>

#### Sass-to-Jess

:::info

The `$!` live-binding sigil in Jess reads the latest (live) value of a variable
rather than its value at that point in the scope. That matches how Sass variable
lookups behave.

:::

<div style={{ display: 'flex', flexDirection: 'row', gap: '1rem' }}>
  <div style={{ flex: 1 }}>
  ```less
  value: $color;
  ```
  </div>
  <div style={{ flex: 1 }}>
  ```less
  value: $!color;
  ```
  </div>
</div>

### Expressions

Jess expressions are always `$(`...`)`. Plain `$var` is a variable reference. Inside an expression, variables keep their `$` sigil — a bare identifier is a keyword, not a variable.

Use:

- `$var` for a direct variable reference
- `$($var)` when you need that reference inside an expression
- `$[ident]` for variable identifier interpolation (selector/property-name interpolation)
- `$['name']` for property lookup interpolation keys (e.g. Less `${name}`)

#### Less-to-Jess

<div style={{ display: 'flex', flexDirection: 'row', gap: '1rem' }}>
  <div style={{ flex: 1 }}>
  ```less
  value: @width + 1;
  value: (@width / 2);
  ```
  </div>
  <div style={{ flex: 1 }}>
  ```less
  value: $($width + 1);
  value: $($width / 2);
  ```
  </div>
</div>

#### Sass-to-Jess

<div style={{ display: 'flex', flexDirection: 'row', gap: '1rem' }}>
  <div style={{ flex: 1 }}>
  ```less
  value: $width + 1;
  value: ($width / 2);
  ```
  </div>
  <div style={{ flex: 1 }}>
  ```less
  value: $($!width + 1);
  value: $($!width / 2);
  ```
  </div>
</div>

### Mixin definitions

#### Less-to-Jess

<div style={{ display: 'flex', flexDirection: 'row', gap: '1rem' }}>
  <div style={{ flex: 1 }}>
  ```less
  .box() {}
  .with-params(
    @param1: 1;
    @param2: 2;
  ) {}
  ```
  </div>
  <div style={{ flex: 1 }}>
  ```less
  .box() {}
  .with-params(
    param1: 1;
    param2: 2;
  ) {}
  ```
  </div>
</div>

#### Sass-to-Jess

<div style={{ display: 'flex', flexDirection: 'row', gap: '1rem' }}>
  <div style={{ flex: 1 }}>
  ```less
  @mixin box() {}
  @mixin with-params(
    $param1: 1,
    $param2: 2
  ) {}
  ```
  </div>
  <div style={{ flex: 1 }}>
  ```less
  box() {}
  with-params(
    param1: 1;
    param2: 2;
  ) {}
  ```
  </div>
</div>

### Mixin calls

#### Less-to-Jess

<div style={{ display: 'flex', flexDirection: 'row', gap: '1rem' }}>
  <div style={{ flex: 1 }}>
  ```less
  .box();
  .with-params(1; 2);
  ```
  </div>
  <div style={{ flex: 1 }}>
  ```less
  $ > .box();
  $ > .with-params(1, 2);
  ```
  </div>
</div>

#### Sass-to-Jess

<div style={{ display: 'flex', flexDirection: 'row', gap: '1rem' }}>
  <div style={{ flex: 1 }}>
  ```less
  @include box();
  @include with-params(1, 2);
  ```
  </div>
  <div style={{ flex: 1 }}>
  ```less
  $ > box();
  $ > with-params(1, 2);
  ```
  </div>
</div>