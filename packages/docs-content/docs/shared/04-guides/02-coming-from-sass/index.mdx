---
title: Coming from Sass
sidebar_label: Overview
audiences:
  - jess
  - sass
origin: shared
slug: /guides/coming-from-sass
---

If you know **Sass/SCSS**, you already know most of Jess. Jess parses SCSS today,
maps its constructs onto a real AST, and — where Sass made a compromise it later
regretted — offers a cleaner equivalent. The supported subset has a name:
**Sass+**, "Sass without the bad parts."

This guide is the path a Sass user takes through the docs: understand what maps,
what is intentionally left out, where Jess is *stricter*, and then how to move a
codebase over.

## Start here

1. **[Sass (SCSS) compatibility](/docs/guides/coming-from-sass/scss-compatibility)** —
   how SCSS syntax maps into Jess: `@use` → `@-compose`, `#{…}` interpolation,
   maps → collections, `map.get()` → reference lookups, placeholders,
   `!global`/`!default`.
2. **[Unsupported Sass+ features](/docs/guides/coming-from-sass/unsupported-sass-features)** —
   the Sass constructs Jess *parses* but will not *evaluate* (`@at-root`,
   `@forward` `show`/`hide`/`as`), and the Jess-native way to get the same result.
3. **[Stricter than Sass](/docs/guides/coming-from-sass/stricter-than-sass)** —
   invalid CSS that Sass tolerates (bogus combinators, escaped directive
   keywords) and that Jess rejects at parse time. Valid CSS is the oracle.

When you are ready to convert a project, see
**[Migrating to Jess](/docs/guides/migrating-to-jess)** for the `jess convert`
first pass and the full Sass → Jess mapping tables.

## The one-line mental model

| Sass | Jess | Notes |
| --- | --- | --- |
| `$color: #06c;` | `$color: #06c;` | Same sigil. |
| `#{$x}` | `$(…)` / `$[…]` | Expression vs identifier interpolation. |
| `@mixin` / `@include` | `box() {}` / `$ > box();` | See [Migrating to Jess](/docs/guides/migrating-to-jess). |
| `@use "./x"` | `@-compose "./x"` | Namespaced module import, isolated scope. |
| `@forward "./x"` | `@-export "./x"` | Forwarding (no `show`/`hide` filtering). |
| `@extend .a` / `%p` | `$extend .a;` | Selector reuse. |

Jess keeps compiler-level at-rules dash-prefixed (`@-compose`, `@-use`,
`@-export`) so they never collide with the evolving CSS standard library. That is
why you see `@-compose` and not a bare `@compose`.
