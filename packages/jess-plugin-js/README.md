# @jesscss/plugin-js

Jess plugin that enables JavaScript/TypeScript module imports.

This package checks for a usable Deno runtime before executing non-`@jesscss/fns`
modules. It is intended to be auto-loaded by `jess` when installed.
