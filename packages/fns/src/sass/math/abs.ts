/**
 * Sass math.abs() function
 *
 * Re-exports the shared abs function for Sass module namespace compatibility.
 * The actual implementation is in shared/math/abs.ts
 */
export { abs as default } from '../../shared/index.js';
