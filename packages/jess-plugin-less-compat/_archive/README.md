# Archived docs (`@jesscss/plugin-less-compat`)

This folder holds **dated snapshots** of design/analysis notes that were previously kept at the package root.

- Goal: keep the package root focused on **current, canonical** docs.
- If you need the old deep-dives, use the dated files here (or git history).

