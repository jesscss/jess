/**
 * Smoke tests for the Parséman-based Less grammar.
 * TODO: migrate to parseLessFn entry point — the LessGrammar builder class
 * no longer has a parse() method; use parseLessFn() / LessParser instead.
 */
import { describe, it } from 'vitest';

describe.skip('LessGrammar (Parséman) — basic smoke tests', () => {
  it('placeholder — re-enable against parseLessFn once migrated', () => {});
});
