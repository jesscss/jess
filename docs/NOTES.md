# Legacy notes

The historical `docs/NOTES.md` dev log was archived to `docs/_archive/` (and remains available via git history).

For current ongoing tracking, prefer:
- `.cursor/changes.md`
- package-local trackers (e.g. `packages/language-service/TRACKER.md`)

