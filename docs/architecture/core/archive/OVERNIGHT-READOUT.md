# Overnight readout — 2026-07-08 (for morning triage)

All landed on `origin/dev` (tip `0ca46136d`), gated byte-identical (alpha `all-less` 90/3 unchanged) + core suite green + tsc 0-new. Nothing risky/gated was implemented unreviewed.

## Landed
- **Node field-budget met.** `Rules` 7→4 class-unique fields (dropped dead `pendingExtends`; folded `lookupVersion`+`varsByName` into lazy `_lookup`). Codebase-wide audit: every node class ≤5 EXCEPT base `Node` (10) — see below. Audits: `RULES_FIELD_BUDGET.md`, `NODE_FIELD_BUDGET.md`.
- **Unified single-eval-emit design finalized** — `UNIFIED-EVAL-EMIT-DESIGN.md`. One downward pass: eval + per-node (canonical-or-fresh shape → inline visitor hook → serialize), live value-frame threaded through emit (the B1s fix), extend PLAN/SOLVE/EMIT folded in. Verdict: **cutover, not incremental**; code-health + correctness, **not a perf lever** (<1% self-time). Absorbs the flag-walk C4 endgame (deletes reuse gates, clone families, container short-circuits, `propagateFlagsFrom`).
- **Extend pipeline complete + validated (bundle-excluded):** `plan.ts` (reachability + target index) → `solve.ts` (document fixpoint, local-apply) → `emit.ts` (compose-relative-to-target + `&`-hoist + collapse/`:is`-grouping). Each reference-validated; **EMIT fixes the nested-extender composed-selector bug** (`.type1 .sidebar3` + `:is(...)` grouping) that current `extend-nest`/`extend-selector` get wrong.

## Decisions waiting for you
1. **Extend production wire-in — CORRECTED: it is NOT a bounded decoupled step.** I framed this as an independent, high-value cutover; a hard-gated staging attempt (branch `work/extend-wirein-staged`, `04005a64e`, unpushed) proved otherwise. Three findings:
   - **Blocker:** `runExtendPipeline` returns selector-header STRINGS; production `processExtends` MUTATES the node graph (`F_EXTENDED`/`F_VISIBLE`/`hoistToRoot` + reference-mode origin flags) and emits `extendNotFound`/`extendNotAccessible` warnings. Bridging strings→node-mutations is exactly the **B1 (placement propagation) + B2 (origin/visible IR annotations)** work the design DEFERS to the render cutover (§5: a naive wire-in broke reference/import mode + `&`-hoist for this precise reason). **So the extend wire-in is COUPLED to the render cutover (decision #2), not decoupled.** My "smaller independent step" framing was wrong.
   - **Coverage reality — RE-CORRECTED (the "`&`-fallback" was a misnomer).** Instrumenting the live corpus: every SUBJECT own-builds (subject-level fallback = 0). The fallbacks are **100% `NO_RS`** — the gathered `Extend` node is *detached from its ruleset after eval* (`.parent` is `undefined`), so the wire-in adapter can't reconstruct the extender's bucket path from the node graph; and real `&:extend(...)` arrives **already amp-resolved** (`target=.sidebar`), never delivering a `&`-carrying find. So the true blocker is **node-detachment / bucket-path reconstruction** — B1/B2 adapter work (recover the path from the pre-composed `extendWith`), cutover-coupled — NOT a `&`-shape gap. (The pipeline's own `&`-find frontier shape WAS own-built — the pipeline is now **shape-complete: capstone 0 UNSUPPORTED / 0 real-gaps / 7 bug-fixes**. Only **2** fixtures fail on dev — `extend-nest`, `extend-selector` — the composed-selector bug the pipeline fixes.)
   - **No perf signal yet:** the staged bench measures the pipeline as ADDITIVE overhead on top of the unchanged apply (within noise on all 4 dimensions), NOT a replacement — the replacement can't be benchmarked without the B1/B2 bridge. So perf questions (fast-reject/chained/clock/memory) remain UNANSWERED for a real swap.
   - The staging (adapter `wire-in.ts` + gated-OFF call + measurement harness) is committed on the branch, **not pushed to dev** (it's blocked scaffolding + a 28kB bundle cost from live-exporting the pipeline; not worth landing until the bridge decision). Recommendation: treat the extend wire-in as PART of decision #2, gated on the B1/B2 node-mutation design — and, before it's worth much, own-build `&`-carrying finds.
2. **Full single-eval-emit render cutover** (the monolith). Multi-week, high-regression, not-a-perf-lever; the frame-threading spine is a coordinated flag-flip, no incremental path (B1s proved it). Go / defer?
3. **Base `Node` (10 fields) → ≤5.** Its fields (`_spanStart/_end`, `_sourceRoot`, `_treeContext`, `_options`, `flags`, `_requiredSemi`, `sourceNode`, `index`, `parent`) are foundational; getting under budget needs deciding which the unified pass still requires per-node (e.g. does every node need `sourceNode`/`treeContext`?). Design-gated on #2. (`_requiredSemi`→flag bit is a cheap 10→9 but high blast radius for little gain — not worth it alone.)
4. **Design open questions:** OQ-A **resolved** (reachability is cleanly single; only interpolated extend *targets* entangle, and they're already broken today — pre-existing, not new). OQ-F **resolved** (per-node inline visitor). Remaining minor: OQ-B (buffer-per-subject vs streaming), OQ-D/E (confluence / `:is`-graft termination bound) — sensible defaults unless you object.

## Not touched (deliberately)
The production cutover(s), base-`Node` reshape, and anything that would change ratified output without your review. The extend pipeline is bundle-excluded (zero production impact) until you decide on #1.
