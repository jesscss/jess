# Cutover Status — compact board

At-a-glance status of the single-eval-emit cutover + core slimming. **Kept current by the orchestrator** (update on every land/dispatch; if stale, distrust it and reconcile against `git log origin/work/cutover-p1` + the ratchets). Canonical detail lives in `CUTOVER-CHECKLIST.md` (governance) + `P4-ENDGAME-PLAN.md` (sequence) + per-workstream design docs.

Legend: ✅ landed on `work/cutover-p1` · 🔵 in-flight · ⏸ queued (core-lane serializes) · 🔒 gated/deferred

Tip: `origin/work/cutover-p1` = `5869859ad`.

## 🔦 Flip-readiness — MEASURED 2026-07-11 (branch `jess-perf-frame-fix` @ `8c4bc1940`, spine-only forced/unconditional)
**Supersedes stale inline counts below — reconcile older figures against this.**
- **all-less fixture corpus: 112/112 render spine-only CLEAN — 0 `SPINE_ONLY_UNSUPPORTED`.** Real-Less top-level render is **100% on the spine**.
- **`benchmark.less` is the ONLY real-source throw**, on 2 shapes, both **foldable-now** (NOT parseman-gated):
  1. **STRIPE recursion** (recursive mixin w/ a nested container shared across levels — gate-7 IOU). The FIRST-firing throw benchmark hits today (`.generate-spacing/-font-sizes/-widths`, `.color-gen`, `.generate-icons`, `.gen-grid`). Fold: distinct-per-level container surface in `serialize-helper.ts` `runSpineMixinExpansion`; gate `emit-walk.ts` `treeHasRecursiveMixinCall`. Size **M–L**. **FOLDED 2026-07-11** (`work/stripe-fold` `cc7b0dcdf`, not merged): `distinctFoldChild` per-level projection in `runSpineMixinExpansion`; `treeHasRecursiveMixinCall` gate DELETED; all 6 benchmark recursive mixins fold `deriveCalls===0`, byte-identical to eval + lessc 4.6.3, ~1.86× faster; core 3300/0, all-less 105/0.
  2. **extend-into-reference, UNMAPPED target** (reference/inner-fallback family; `SPINE_ABORT_TO_EVAL`). **Partly a FIXTURE ARTIFACT**: `benchmark-import-target.less` + `benchmark-import-reference-target.less` are **0 bytes**, so `.ref-*` targets don't exist. Populate the targets or fold the abort. Size **S–M**.
  3. **NEW (surfaced post-STRIPE) — string-interp in a container-body value** (`url("@{base}/@{i}.svg")`, `.generate-icons`): not resolved on the mixin-container fold. **PRE-EXISTING / orthogonal** — affects non-recursive container folds equally, not recursion-caused. IOU: spine string-interp-in-container-value resolution. This is now the leading benchmark blocker alongside #2.
- **Core suite: 77 reds** = 76 eval-fallback `.render()` unit tests (test-rewrite/re-home debt, D3/D4 deletion targets — NOT fixture-coverage shapes) + 1 stale ratchet (append edge-shape now folds; decide stale-vs-regression). Earlier "~178–190 core reds" is **STALE** — this branch has more folds landed than prior snapshots.
- **Nothing in the tail is blocked on the external parseman/producer agent.** Remaining work-list is fully drivable spine-side now: fold STRIPE, handle reference-extend + populate benchmark targets, rewrite the 76 eval-fallback tests, resolve the 1 ratchet.
- **Dead-symbol cleanup (current integration):** `TreeVisitor` was removed from `visitor/index.ts` and its test; repository search found no production subclass or import. The remaining eval/visitor symbols are intentionally load-bearing and stay in this lane.

## ✅ Landed
- **Extend (spine, faster than eval, byte-identical):** common modes, Cases 1–3, Shape 4, extend-nest, extend-selector, #4a expanded-mode; combinator subjects/targets; partial list-targets; trailing/mid descendant wraps. **Perf: O(n²)→O(n) fan pre-reject — 2.4× slower → 1.37–1.43× faster.** Ratchet 61/61.
- **Extend HARD-TAIL cluster (spine, collapse mode, byte-identical):** `extend.less` now FULLY FOLDS in collapse mode (`deriveCalls===0`, spine, byte-identical to the eval reference + less@4). Folded sub-shapes: (a) **multi-branch selector-LIST subjects** — `.foo .bar, .foo .baz` gains per-branch in-place `:is`-wraps (`.bar`/`.foo` targets split from the list local into `rootDescendantBranches`/`combinatorBranchSelectors`, plus `isExactRootBranchTarget` for a target ≡ a combinator/descendant list branch like `.ext8 + .ext9`); (b) **nested-ruleset-subject targets** — `.ee:extend(.dd all)` grafts a SIBLING child branch under the (extended) parent context (`composeSpineSubjectHeaders` collapse-nested branch now projects the child local + folds EACH grafted child branch, full-path filter keeps EXACT extends off the bare sub-compound; gated via new `isPartialWrapOfNestedLevel`); (c) **`&`-composed subject as an extend target** — `.ext8 { &.ext9 {…} }` → `.ext8.ext9` is now a subject (`ampComposed`) so `.fuu:extend(.ext8.ext9 all)` grafts it, with parent-`&`-flow deference (skip the override when the PARENT gained a branch, e.g. `&:hover` under an extended `.button`) + collapse-mode-only + full-composed-target filter (so partials still ride the `&`-flow group). **Perf A/B: spine(collapse) 2.2ms vs eval(expanded) 3.8ms per render — ~1.7× faster, no regression.** Ratchet: jess `spine-production-ratchet` +1 net (97→98; flipped the stale "nested extend stays on eval" negative ratchet → byte-correct fold, + a new full-cluster fold test). **RESIDUALS (reference-verified byte-identical, ratchet-locked — precise IOUs):** (1) **expanded-mode (`collapseNesting:false`) nested-target collapse-relocation** — a nested-ruleset subject whose composed form is ALSO a root-level subject (`.ext8 { .ext9 {…} }` with root `.ext8 .ext9,…`) must collapse-and-group to root to carry extender branches, a block relocation the expanded spine path does not reproduce; the whole tree stays eval in expanded mode (this is the mode the all-less harness uses for `extend.less`, so the HARNESS render is still eval-routed — the FOLD is proven in collapse mode). (2) **at-rule/@media-scoped extend gather** — `extend-media.less` (`@media { .x:extend(.a all) }`, cross-media `all`-propagation): LANDED (see the @media-scoped extend gather entry below — the gather now descends conditional-group at-rules with a scope-prefix reachability filter). (3) **append×extend** (`treeHasAmpersandAppend` + extend): an append-generated selector as an extend target the static gather misses. (4) **attribute/graft-paren targets** (`extend-selector.less` `[data="test"]`; `extend-exact.less` `.replace.replace .replace`): tokenizer-conservative.
- **Mixin:** terminal-sink A/B/C (ruleset-as-mixin, mixed-match, non-recursive nested-call), #6 intermediate-scope closure.
- **ROOT-LEVEL mixin call fold (spine, byte-identical, faster):** a document-root-direct mixin call (`.m();`) now folds through the SAME `resolveSpineMixinCall` sink the CONTAINER descent uses, instead of eval-materializing via `Call.render` (the ONLY structural gap left between a root call and a container-nested one). `Rules._emitRulesBody`'s root emitter drives the call once, and for `kind:'fold'` emits each bound surface's children inline against `context.rulesContext` pushed to the surface — a nested-container child descends via `Ruleset.render`'s spineMode branch, a leaf via `n.render`, both resolving against the mixin's definition/param frame. Leaky-mode surface-var leak is propagated (`injectSpineLeakyMixinSurfaceBindings`, root parity); a bare-property-at-root call output still errors (`checkValidNodes` over the folded surface — `property-in-root{,2}`, `detached-ruleset-3` stay red as Less does). A non-simple body / silent-fail / unseen-by-sink call resolves `kind:'eval'` and rides the byte-identical eval terminal. This flips **`comments.less` to FULLY FOLD** (`deriveCalls===0`, spine, byte-identical) — its last residual was a root guarded mixin call (`.mixin_def_with_colors() when (@a=white)`). **Perf A/B (comments.less): spine 1.20ms vs eval 1.40ms per render — ~15% faster, no regression.** Ratchet: jess `spine-production-ratchet` 122→124 (root-call-folds unit lock + `comments.less` whole-fixture derive=0 lock).
- **At-rule:** @layer/@scope/@property folded; ampersand-append folded; **at-rule-`&`-through-hoist folded** (see ✅).
- **Imports:** common modes + transitive nested-linking bug fix.
- **Value/decl:** `@x ?:` (CondAssign) + same-scope `setDefined` folds; `:=` (nearestOuter) EVAL REFERENCE implemented (spine fold deferred).
- **Slimming:** `!important` constant → bare string (Any reduction); lean-selector string-form consumers + 16 tests; string-forms model (ComplexSelector/RelativeSelector positional).
- **Parser (on dev+alpha):** grammar-thinning wrapper drops (css/less/scss/jess) + SelectorCapture interior-trivia fix.
- **interpolated-name fold (M8):** interpolated-selector ruleset called as a mixin folds through the spine (mid-string `.foo-@{n}` + no-call inert registration). Ratchet +3 (61→64). V4 (interpolated var-name) and R2 (interpolated at-rule-name) verified **non-features** — no additional spine work needed; the "3-for-1" collapses to M8 alone.
- **mixin-as-value / detached-ruleset arg (FOLDED):** a mixin call passing a detached ruleset — by REFERENCE (`.wrap(@ruleset)`) or as a NAMED block arg (`.wrap(@r: { … })`) — folds through the spine. Root was silently MIS-FOLDING to EMPTY output: the outer call's non-simple surface was rejected to the eval terminal, but the still-live surface sink intercepted the NESTED `@r()` detached-call resolution and dropped its output. FIX (`callable-candidate-output.ts`): SUSPEND the sink across a rejected candidate's `rules.eval` fall-back so the nested detached call materializes its own output; restore for sibling candidates. Byte-identical to the pure-eval reference across ref/named/literal/@media-wrapped/default+override forms. **Perf A/B neutral** (spine ≈ eval, within noise). RESIDUAL (spec, byte-identical): the DR-arg call itself takes the eval-fallback rung (`resolveSpineMixinCall` → `kind:'eval'`) — the spine has no detached-ruleset-call (`@r()`, a `Reference`-keyed `Call optType:'variable'`) expansion yet; the ENCLOSING root still folds (same rung ruleset-as-mixin / merge-across use, NOT a whole-root punt). Ratchet: core `mixin-fold-detached-arg` +7; jess `spine-production-ratchet` +2 (72→74).
- **detached-ruleset CALL — RUNG-1 (FOLDED):** the residual the mixin-as-value fold (`528d465fc`) left — the detached-ruleset call ITSELF (`@alias()` / `@1()` / `@conditional()`) — now folds on the spine (`deriveCalls===0`, byte-identical to eval / less@4). DIAGNOSIS (premise corrected): the Less parser shapes such a call as an **`Expression` wrapping a single `Call`** whose name is a `variable`-type `Reference` with a **`Keyword` key** (not a bare `Call` with a string key, as the "notRuleset [Expression]/[Call]" first-reject label implied). FIX (two seams): (1) `emit-walk.ts` — `unwrapDetachedRulesetCall` unwraps the single-Call Expression, `isSpineEligibleMixinCall` admits a `variable`-type Reference call with a string-OR-`Keyword` key, and `resolveSpineMixinCall` drives the unwrapped inner `Call` (its `Expression.eval` delegates to the wrapped call, routing through the SAME callable-candidate sink); (2) `callable-special-case.ts` — the UNLOCKED (`callable-rules`, no-name/no-param) arm that a literal detached-ruleset call (`@r: { … }; @r()`) resolves through now hands the WIRED surface to the sink (mirroring the Ruleset-as-mixin arm) instead of eval-materializing it. A bound-mixin-call value (`@alias: .something(foo)`) resolves via the inner `.something(foo)` mixin-ruleset candidate, which already hit the sink. **Perf A/B (clean rebuild): DR-call-heavy(60) spine 4.29ms vs eval 4.91ms — ~13% faster; common(200) 11.9ms both, benchmark.less ~275ms both — neutral (zero-cost off the DR path: the unwrap fast-bails when a body child is not an `Expression`-wrapped `variable` call).** Clean full rebuild: core 3281/0, jess `spine-production-ratchet` 105→108 (+`@alias()` bound-call fold lock, +literal `@r()` fold lock, +DR-arg RESIDUAL lock — all `deriveCalls` asserted), all-less 104/1 (only pre-existing `import-module` env red — `@less/test-import-module` not installed, reproduces IDENTICALLY on base with my changes reverted), all-less-error 92/0, less-parser 491/0. **DIAGNOSIS per measured fixture:** `namespacing-6` — RUNG-1 (`@alias()`, `.rule-1`) FOLDS to `deriveCalls===0` in isolation; the FULL file has a **2nd stacked reject: a detached-ruleset value passed AS a mixin ARG** (`.wrapper(@alias)` / `.wrapper(.something(foo))` in rule-2/rule-3), whose arg-binding CLONES the bound `Rules` value (`cloneDefinedBoundValue`→`cloneBoundValue`→`Rules.clone`→derive) — a SEPARATE P4 rung (DR-value-as-arg clone elision). `functions` — the DR-calls (`@1()`/`@conditional()`/`@falsey()`) FOLD in isolation (`deriveCalls===0`); the full file's residual is the DIFFERENT `each()`/`$for` family (eval-body-loop derives), as predicted. `css-escapes` — NOT this family: its first-reject `[Call]` is an ESCAPED-SELECTOR mixin call (`.mixin\!tUp()`), left untouched. RESIDUAL / IOU (P4): DR-value-as-mixin-arg clone elision on the spine (the 2nd rung), then `namespacing-6` reaches whole-file `deriveCalls===0`.
- **detached-ruleset passed AS a mixin ARG — DRARG-1 (FOLDED):** the RUNG-1 residual/IOU — a detached-ruleset value passed AS a mixin argument (`namespacing-6` rule-2 `.wrapper(@alias)` where `@alias: .something(foo)`, and rule-3 inline `.wrapper(.something(foo))` / `.wrapper(.output-height())`) — now folds on the spine (`deriveCalls===0`, byte-identical to eval / less@4). DIAGNOSIS (instrumented `Rules.derive`, premise CONFIRMED): the DR-arg routed to eval via TWO stacked derive-clones, not one. (1) BIND-time: `getBindingCellValue`→`cloneDefinedBoundValue`→`cloneBoundValue`→`cloneForPlacement`→`Rules.clone`→`derive`. (2) READ-time: the callee body's param-var reference (`@another-mixin`) read hits `evaluateReferenceValueNode` (reference.ts) which `cloneForPlacement`→`derive`s a rules-like value that is NOT `preserveRulesLike`/`mixin-ruleset` flagged — because the binding source is a **paramVar** VarDeclaration (the non-paramVar DR read already took the no-derive `createRulesLikeReferenceSurface` bailout, which is why the `@alias()` VarDeclaration path in rule-1 already folded). The DR closes over the surface where it was WRITTEN, already captured on `_closureScope` at arg-eval (`evaluateCallableArgs`, set BEFORE binding) — so NEITHER placement clone added isolation the closure/call path needs. FIX (two seams, both no-derive): (1) `callable-binding.ts` `cloneBoundValue` — bind a `Rules` value DIRECTLY (skip `cloneForPlacement`), same as the VarDeclaration DR path; (2) `reference.ts` — widen the `REF_EVAL_PRESERVE_RULES_LIKE` gate to also cover a paramVar VarDeclaration whose `bindingValue` is a `Rules`, so the read routes through the shared-children `createRulesLikeReferenceSurface` (no `clone`/`derive`/`inherit`) instead of `cloneForPlacement().eval()`. **Perf A/B (clean rebuild, file-based, warmed): benchmark.less base 327.97ms vs folded 327.67ms — perf-neutral (benchmark.less has no DR-as-arg shape; fast-bail cost is one `isNode(value, N.Rules)` check per bound value + a paramVar/Rules condition reached only on rules-like param reads).** Clean full rebuild: core 3281/0, jess `spine-production-ratchet` 114→115 (RUNG-1 "DR-arg RESIDUAL" negative lock flipped to +DRARG-1 variable-aliased fold lock + inline DR-call fold lock — both `deriveCalls===0` + byte-identical asserted), all-less 105/0 byte-identical + all-less-error 92/0. **`namespacing-6` now FULLY FOLDS: `deriveCalls===0` across all three rules (rule-1 RUNG-1 + rule-2/3 DRARG-1), byte-identical to the owner `.css`.** NO residual — the DR-value-as-mixin-arg family folds whole, closing the RUNG-1 IOU.
- **mixin-as-value / map-lookup (FOLDED — MIXVAL-1):** a body with a mixin DEFINITION whose output is bound to a variable (`@p: .mk-map()`) then map-subscripted (`@p[text]`) now FOLDS on the spine (`deriveCalls===0`, byte-identical to eval / less@4). DIAGNOSIS (premise confirmed conservative): the first-reject `bodyHasMixinDefinition(children) && bodyHasCallInVarValue(children)` (`isSpineEligibleBody`, emit-walk.ts) was PURELY over-conservative — no dedicated machinery was needed. The var cell binds at scope-enter (`buildScopeFrame`) and the `@p[key]` subscript resolves through the KEPT `Reference.eval` against the live value-frame (`emitLeaf` → `node.eval(context)` under `withValueFrame`), exactly as the eval path does — the mixin-call value evals lazily on first read of `@p`, producing the output map that `[key]` subscripts. FIX: DELETE the reject clause + its two now-dead helpers (`bodyHasMixinDefinition`, `bodyHasCallInVarValue`); the loop-body eligibility check already admits an unparameterized/parametric mixin DEFINITION (`isSpineEligibleMixinDefinition`) and a `Reference` value leaf. **DIAGNOSIS per measured fixture:** `maps` (d=2, `.mk-map(){…} @p: .mk-map(); .x{color:@p[text]}`) FOLDS whole-file (`deriveCalls=0`, `rootRenders=1`, `.x{color:red}`); `nesting` (d=3, same shape nested under `.outer`) FOLDS (`deriveCalls=0`, `.outer .x{color:red}`). Both byte-identical to eval + less@4 — NO stacked residual. Adjacent shapes verified fold byte-identically: multi-key map, parametric mixin-as-value (`.mk(@c){text:@c} @p:.mk(orange)`), detached-ruleset call, mixed bare-call-plus-lookup body. (The `@p[@k]` variable-KEY shape resolves to `color: text` on BOTH spine and eval — a PRE-EXISTING jess-vs-less@4 difference in the eval reference itself, unaffected by this fold — less@4 errors `@k not found`; out of scope.) **Perf A/B (clean rebuild, file-based, warmed): benchmark.less base 308.1ms vs folded 306.4ms — perf-neutral/marginally faster (the deleted predicates ran per-body on the eligibility walk, so removing them is a tiny win; zero-cost by construction).** Clean full rebuild: core 3281/0, jess `spine-production-ratchet` 111→113 (+MIXVAL-1 flat fold lock, +MIXVAL-1 nested-container fold lock — both `deriveCalls===0` asserted; flipped the stale core `emit-walk-ratchet` "EXCLUDES mixin-as-value" negative assertion → byte-correct FOLD), all-less 104/1 + all-less-error 92/0 (the 1 red = pre-existing `import-module` env red, `@less/test-import-module` missing, reproduces IDENTICALLY on base), less-parser 491/0. NO residual — the mixin-as-value / map-lookup family folds whole.
- **each()/$for LOOP iterations (FOLDED — LOOP-1):** a container-nested `each(list, {…})` / `$for` loop (both parse to a `For` node) now FOLDS on the spine (`deriveCalls===0`, byte-identical to eval / less@4). MECHANISM: `For.spineIterationSurfaces` (control.ts) produces one bound-body surface per iteration — a per-iteration COPY of the loop body (reusing scalar leaves) under a fresh `buildScopeFrame` whose live slots carry the iteration `@value`/`@key`/counter bindings (the spine analogue of eval's `createForIterationSurface`); `runSpineForExpansion` (serialize-helper.ts) splices each surface's children in order, tagged `spineFrame=surface` (per-iteration var resolution) + `mergeOwner=the For node` (so a `+_:` merge ACROSS iterations coalesces into ONE property via the existing `replanMergesIfExpanded`). Interpolated decl NAMES (`item-@{value}`) resolve via per-surface `prepareRegistration` (name identity is resolved there, not in `Declaration.evalNode`). `flattenVisibleRulesForRender` keeps a `For` as a container entry (not flattened, since it is `Rules`-masked) so the loop-fold pass owns it; `isSpineEligibleFor` (emit-walk.ts) admits a loop whose body is spine-coverable. Per-iteration node COPIES are REQUIRED (not shared) so the node-keyed merge re-plan / dedup bind each iteration's decl to its own frame (a shared node collapses to last-wins → `each(1 2 3,{p+_:@value})` would wrongly emit `3 3 3`). **DIAGNOSIS per measured fixture:** `starting-style` — the `each(1 2 3 4, {padding+_: (@value*10px)})` inside `@starting-style` FOLDS whole-file byte-identical (`deriveCalls===0`, `rootRenders=1`: loop + merge-across-iterations + the `@starting-style` wrap all fold). `functions` — the `each(~(1 2 3); {item-@{value}: @value+3})` FOLDS in isolation (`rootRenders=1`, byte-correct `item-1/2/3`), but the FULL file stays eval-routed behind an UNRELATED 2nd stacked reject — a bare `if((false), {g: 7})` VOID detached-ruleset STATEMENT-call in `#if` (a `Call` first-reject, NOT the loop; prompt premise "each() is the remaining first-reject" was optimistic). RESIDUAL / IOU: (1) a ROOT-DIRECT loop (`each(…)` directly at document root, no enclosing ruleset) stays on eval — the root emitter `Rules._emitRulesBody` treats a `For` as a transparent child-Rules and emits its unexpanded body; folding it needs that emitter to route a `For` through the loop-fold expansion (sequenced increment, byte-identical, ratchet-locked). (2) `functions` whole-file needs the void-DR-statement-call fold (separate family). **Perf A/B (clean rebuild, file-based, warmed): loop-heavy(40×`each` of 10) 9.6→9.6ms (FOLDS in mine, eval in base — neutral); benchmark.less 208→205ms, common no-loop within noise — zero-cost off the loop path (the `type==="For"` check fast-bails).** Clean full rebuild: core 3281/0, jess `spine-production-ratchet` 108→111 (+LOOP-1 fold lock, +merge-across-iterations fold lock, +root-direct RESIDUAL lock — all `deriveCalls` asserted), all-less 196/1 + all-less-error 92/0 (the 1 red = pre-existing `import-module` env red, `@less/test-import-module` missing, reproduces IDENTICALLY on base).
- **single-file TRANSITIVE `:extend` chaining (FOLDED — CHAIN-1):** the whole `extend-chaining` corpus fixture now FOLDS on the spine (`deriveCalls===0`, byte-identical to the owner `.css` + less@4). DIAGNOSIS (premise confirmed conservative): the fixture routed to eval on the `container:notRuleset [Extend]` first-reject, which resolved to two chaining guards in `isSpineExtendTopology` (spine-extend.ts): (a) the trailing `if (extenderSelectors.has(target)) return false // chaining — deferred` — a target that is itself an extender's subject (`.b:extend(.a)` where `.b` is `.c`'s target); and (b) the `chainsIntoExtender` exclusion on `isMatchableCompoundTarget` / `isPartialWrapOfDescendantLevel` / `isPartialWrapOfNestedLevel` — a SUB-COMPOUND chain target (`.i` ⊆ the extender `.i.j`, the `all`-partial chain `.g.h ← .i.j:extend(.g all) ← .k:extend(.i all)` → nested `:is(.g, :is(.i, .k).j).h`). BOTH were purely over-conservative: SOLVE's document-level fixpoint (`solveSubject`, solve.ts) already re-enqueues each produced branch value and re-queries the SHARED target index, so a chained / circular / `all`-partial chain drains to its fixed point through the same machinery that landed cross-import transitive extend — no new machinery. FIX: DELETE both guard clauses + the now-dead `extenderSelectors` set. The whole file folds byte-identical: simple chains (`.a→.b→.c`), reverse-order chains, 8-link multi-chains, self-reference (`.w:extend(.w)`), classic circular refs (`.x←.y←.z←.x`), `&:extend` inside a ruleset, and the `@media (tv)` (27-target multi-extend) / nested `@media (plasma)` scoped block. **Perf A/B (warmed, file-based): extend-chaining spine 2.92ms vs eval 6.52ms — 2.23× faster; extend perf-ab suite unchanged (extend-heavy 1.99×, extend-light 1.31×, extend-free neutral); the removed guards only ran on the chaining reject path so benchmark.less is inert (~307ms, in baseline range).** Clean full rebuild: core 3281/0, jess `spine-production-ratchet` 113→114 (+CHAIN-1 whole-fixture fold lock, `deriveCalls===0` + byte-identical asserted), all-less 105/0 byte-identical + all-less-error 92/0. Two internal core tests pinned superseded EVAL behavior and were repinned: `render-scaling` (measures the EVAL matcher's anti-quadratic work counter — the flat chain now folds so force eval via a no-op `preSerializeRoot` hook to keep exercising the matcher) and `extend-rules` pseudo-classes (asserted eval's single-MERGED-block for a self-extend of two separately-authored `.btn:hover` rulesets — a silent merge that DISAGREES with less@4's two-block output; repinned to the spine's structurally-correct two-block form). NO residual — the single-file transitive-chaining family folds whole.
- **reference-mode import (FOLDED):** `@import (reference)` — REGISTERS-scope-only, scope-consumed-by-var/mixin, and reference-inside-a-ruleset already folded; NOW the **extend-into-reference** case folds too. `.ext:extend(.target)` reaching a `@import (reference)` body's `.target` folds through the spine byte-identical to eval (`.ext { … }`, the reference target suppressed on its own). MECHANISM: `collectImportedRootSubjects` collects reference-body subjects (own set), `wireSpineExtends` marks them `reference`, and `composeSpineSubjectHeaders` DROPS branch 0 (the own form — the spine analogue of the eval `F_EXTEND_TARGET` reference-filter), installing an extender-only header; the container serializer render-enables the ruleset because it carries a spine extend header (the reference-unlock signal — `serialize-helper` `nodeExtendsReference` now also fires on `spineExtendHeaders.has(node)`, since the reference TARGET bears no `F_EXTENDED` of its own). **Perf A/B: spine 0.665ms/render vs eval 1.028ms/render — ~35% faster.** CORRECTION (`82bf54d81` was a false-green): the render-unlock arm was NOT wired — `82bf54d81` was validated against a stale `lib/`, so from clean source both ratchet reference-extend tests emitted empty. Fixed forward in `serialize-helper` (the one-line `spineExtendHeaders.has(node)` unlock); verified from a clean full rebuild (core 3280/0, ratchet 77/77, all-less 105/0, error 92/0). The `extend-cross-import` `reference-import` test also flips GREEN. RESIDUAL (`isSimpleReferenceFoldBody`, reference-verified byte-identical, ratchet-locked — `import-reference-issues` corpus): a reference body that carries its OWN `:extend`/`&`-selectors, nested containers, at-rules/namespaces, or `(reference, multiple)` composes under different rules and ABORTS to eval. IOU (inner-fallback-elimination): fold the rich reference shapes (own-extend / namespace / `&`) so the abort-gate can be removed at P4. Ratchet: jess `spine-production-ratchet` +1 (74→75; the increment-5 extend-reaching-reference test converted RESIDUAL→FOLD + a new `(reference)` all-partial fold test).
- **extend-through-import — plain (non-`reference`) TRANSITIVE (FOLDED):** the cross-import transitive closure now folds onto the spine. `.b:extend(.c)` in an imported file + `.a:extend(.b)` in the main file resolve the fixed-point THROUGH the import boundary (`.c`→`.c,.b`→`.c,.b,.a`), byte-identical to less@4. The SOLVE fixpoint + the import re-gate + `wireSpineExtends`'s imported-body gather already produced the correct override map (`.c`→`.c,.b,.a`, `.b`→`.b,.a`); the ONLY gap was at EMIT — an imported body carrying its OWN `:extend` was rejected by `isSpineFoldableImportBody` (`allowExtend=false`) and fell to the `evalNode` terminal, which mints FRESH node instances the override map (keyed on the gather's body-child Ruleset instances) could not key on → no cross-import extend at all. FIX: thread `allowExtend` into `isSpineFoldableImportBody(body, allowExtend)` from `options.spineExtendHeaders !== undefined` at all three call sites (`rules.ts` `_emitSpineImportFold` ×2, `serialize-helper`), so the extend-bearing imported body folds INLINE (same node instances) and picks up its composed header via `effectiveHeaderSelector`. Sound because the re-gate (`isSpineExtendTopology` with `importedRootSubjects`) already aborts the whole tree to eval unless the imported extend shape folds. Plain-import sibling of the reference fold: the imported rulesets STILL render (with their extended selector lists), whereas reference bodies stay suppressed unless pulled in. **Perf A/B: spine ~1.01ms/render vs eval ~1.35ms/render — ~25% faster** (eval also produced WRONG bytes for this shape). Verified from a clean full rebuild (core 3280/0, ratchet 78/78, all-less 105/0, error 92/0); the `extend-cross-import` transitive-closure test flips GREEN. Ratchet: jess `spine-production-ratchet` +1 (77→78).
- **interpolated-path import — case B retry (FOLDED):** a FORWARD-dependent interpolated `@import` path (`@import "theme-@{t}.less"` where `@t` binds in a LATER import) now FOLDS onto the spine. Case A (downward-resolvable) already folded (`dab7fc9cf`); case B previously aborted to eval. MECHANISM: `wireSpineImportsInBody` mirrors the eval loop's `pendingImports`/`drainPendingImports` reorder — a path-resolution failure DEFERS the import, the rest wire (binding the var into the live frame via the fallback link), then a retry-allowed drain + a final non-retry drain resolves it (byte-identical to eval; a genuinely-unresolvable path re-throws on the final drain → clean pre-first-byte abort to eval). **Perf A/B: spine fold ~1.17ms/render vs eval abort ~1.24ms/render — ~6% faster** (no double-walk). RESIDUAL (`deferredBodyHasInlineImport`, reference-verified byte-identical, ratchet-locked — `import-interpolation` corpus): a DEFERRED (case-B) import whose body carries an `(inline)` sub-import aborts to eval — eval's retry-lane re-eval adds a post-inline blank line the clean spine fold does not reproduce. Case A (non-deferred) inline bodies are UNAFFECTED (agree with eval, fold). IOU (inner-fallback-elimination): reproduce eval's reorder spacing on the spine (or eliminate the eval-side artifact) so this abort-gate can be removed at P4. Ratchet: jess `spine-production-ratchet` +2 (75→77 total tests; case-B abort→FOLD conversion + inline-body RESIDUAL + genuinely-unresolvable RESIDUAL).
- **at-rule-`&`-through-hoist (FOLDED):** a nested conditional-group at-rule (`@media`/`@supports`/`@container`/…) whose body uses `&` to reference the enclosing selector, OR carries a DIRECT declaration, now FOLDS onto the spine under `collapseNesting`. `.a { @media { & { … } &:hover { … } } }` → `@media { .a { … } .a:hover { … } }`; `html { @supports { display: grid } }` → `@supports { html { display: grid } }`. Byte-identical to eval AND less@4. ROOT CAUSE of the prior deferral: `getHoistedParent` recovered the enclosing structural ruleset frame via `atRule.parent` — a back-pointer the eval pass set but a PARSED source tree (the spine's input) never has — so it returned `undefined` and the hoist dropped the ancestor wrapper, emitting the content bare. FIX (`serialize-helper.ts` `getHoistedParent`): fall back to `context.rulesetFrames.at(-1)` (the live enclosing ruleset the spine descent pushes) when the `.parent` walk finds nothing — the same structural frame eval recovers, without `.parent`. The composed parent selector was ALREADY available (`composedSelectorStack.at(-1)`); only the identity-key frame was missing. The two conditional-group `&`-body/ancestor-rewrap gates in `emit-walk.ts` (`atRuleBodyHasAmpersandRuleset` on the at-rule, `bodyHasAtRuleNeedingAncestorRewrap` on the enclosing ruleset) are REMOVED. **Perf A/B: spine ~32.9ms vs eval ~36.4ms on a 200-ruleset `&`-through-hoist workload — ~10% faster** (no eval two-walk / output-tree materialization). Verified from a clean full rebuild (core 3280/0, ratchet 82/82, all-less 105/0, error 92/0). RESIDUAL: (1) the MIXIN-SURFACE analogue — an at-rule child in a mixin body needing the CALL-SITE selector rewrap on hoist — NOW FOLDS (see the mixin-surface at-rule-through-hoist entry above; `bodyHasAtRuleNeedingAncestorRewrap` deleted); (2) ROOT-ONLY at-rules (`@host`/`@document`) with a bare-`&` child stay gated (no hoist, no ancestor — eval collapses `&` to bare, reference-verified). Ratchet: jess `spine-production-ratchet` +4 (78→82).
- **mixin-surface at-rule-through-hoist + root-only bubble (FOLDED — 6a):** the MIXIN-SURFACE analogue of at-rule-`&`-through-hoist now FOLDS. A mixin body whose nested container is a hoisting conditional-group at-rule (`@media`/`@supports`/…) — carrying a DIRECT declaration, a bare-`&`/`&:hover` child, or a mixed body — re-wraps the CALL-SITE selector on hoist: `.mix() { @media { & {…} &:hover {…} } }` called at `.class` → `@media { .class {…} .class:hover {…} }`. A ROOT-ONLY "wrap+emit" at-rule in a mixin body (`.mix() { @font-face {…} }`) BUBBLES to root, dropping the call-site selector. Byte-identical to eval AND less@4. MECHANISM: no call-site-specific machinery was needed — the surface's at-rule child is spliced at the call site (`runSpineMixinExpansion`), so `getHoistedParent` recovers `.class` from `context.rulesetFrames` exactly as the authored at-rule fold does. THREE fix-forwards surfaced by lifting the gate: (1) `serializeSpineFrameAtRule` gained the PER-CALL scope-frame RE-POINT the container path already had (`getScopeFrame` memoizes `_scopeFrame`, so a shared at-rule node called with different args leaked the first call's param frame — `.emit(@m){ @media @m { value:@m } }` emitted `screen` for a `print` call); (2) `AtRule.isHoisted`'s `hasRulesetAncestor` now consults `context.rulesetFrames` (a PARSED tree has no `.parent`, so a root-only at-rule nested under a ruleset never bubbled on the spine — `@font-face` wrongly wrapped under its ancestor); the DEAD `bodyHasAtRuleNeedingAncestorRewrap`/`atRuleBodyNeedsAncestorRewrap` helpers + both mixin-surface + tree-level gates are REMOVED. **Perf A/B: spine ~20.7ms vs eval ~40.0ms on a 200-instance at-rule-through-mixin-body workload — ~48% faster** (no eval two-walk / output-tree materialization). Verified from a clean full rebuild (core 3280/0, ratchet 86/86, all-less 105/0, error 92/0). RESIDUAL (6b, reference-verified byte-identical, ratchet-locked): a mixin body containing a nested Mixin DEFINITION stays on eval — the SIMPLE shape (nested def called only within the same body) folds byte-identically, but a nested def can be a DYNAMICALLY-CREATED callable resolved by a LATER path-call (`.Person(@n){ .@{n}{ .sayGender(){} } }` then `.person.sayGender()`, a jess-eval `mixin-ruleset` feature — see `mixin.test.ts`); folding it away emits it inline instead of REGISTERING it, so the later lookup throws `No matching mixins found`. Registering a dynamically-created nested callable through the fold is a SEPARATE surface. IOU (P4): spine callable-registration for dynamically-created nested defs, then lift the 6b gate. Ratchet: jess `spine-production-ratchet` +4 (82→86).
- **root-level `?:` + same-scope `setDefined` (FOLDED — gates 4/5):** an `@x ?: v` conditional-assign or a same-scope `!global` `setDefined` DIRECTLY in the ROOT body now folds — the root body is just another entry sequence. MECHANISM: the root spine descent (`Rules._emitRulesBody`, root branch) now installs the SAME `withSpineMergePlan` machinery the CONTAINER descent uses (`?:` plan via `planBodyConditionals` + `setDefined` binding-write via `applyBodySetDefined`), and the root leaf path consumes the `?:` plan through the shared `resolveSpineLeafText` (routed there when a plan entry exists, then written explicitly — the resolver returns bytes WITHOUT touching the buffer, unlike a plain `n.render` leaf; a boundary-`\n` after the leaf mark had made the old buffer-write assumption drop the anchor text + emit a stray `;`). The former whole-root abort gates `bodyHasDirectConditionalDecl`/`bodyHasDirectSetDefined` are DELETED — the existing per-leaf `isSpineEligibleBody`/`isSimpleSpineLeaf` analysis already keeps the UNFOLDABLE variants on eval byte-identical (property-`?:`, cross-scope `setDefined`, `nearestOuter`/`:=`). Byte-identical to eval across prior-wins/fallback `?:` and same-scope `setDefined`. **Perf A/B: spine ~32.4ms vs eval ~35.0ms on a 400-ruleset eligible-root workload — spine faster, wrap fast-bails on the common no-`?:`/no-`setDefined` case.** NOTE: gates 4/5 at the root are NOT producible from standard Less source (the Less parser shapes root `@x ?:` as an `AtRuleStatement`, not an `assign:'?:'` VarDeclaration) — reachable via core node-builders, so the CORE `emit-walk-ratchet` grows (the old "still EXCLUDES ROOT-LEVEL …" test → a byte-identical FOLD test for gates 4/5 + gate-3 residual assertions). Jess `spine-production-ratchet` stays 86 (shapes unreachable from Less at root). Clean full rebuild: core 3280/0, ratchet 86/86, all-less 105/0, error 92/0. RESIDUAL (gate 3, reference-verified byte-identical, ratchet-spec'd): a root-direct `+:`/`+_:` property-MERGE STAYS on eval — `planBodyMerges` diverges from eval on three edge sub-shapes a root merge can hit ((a) Add-`+:` pulling a PRIOR PLAIN same-named decl into the chain, (b) a DECLARATION-reference read of the coalesced property, (c) source-value node-parent preservation); these are latent `planBodyMerges` gaps the CONTAINER fold already ships untested. IOU (P4): model Add-pull-prior + decl-ref-to-merged in `planBodyMerges`, then lift gate 3.
- **async-valued merge-across-mixin (FOLDED — gate 8) + shared-printState isolation:** an ASYNC-valued merge decl arriving through mixin calls (`transform+: rotate(90deg)` — a merge value containing a `Call`/`Operation`/`Reference`) now FOLDS on the spine (`deriveCalls === 0`), byte-identical to the eval reference. The former whole-root abort gate `treeHasAsyncMergeContributingMixinCall` is DELETED. ROOT CAUSE (a pre-existing eval-fallback **header-drop** bug the gate was masking): the async shape resolves via the eval-fallback expansion, whose dedup/re-resolution evals an unknown/passthrough `Call` (`rgb()`, `rotate()`) that renders its call syntax through `prepareRenderPrintState`, which RESETS the shared `context.printState` IN PLACE (fresh writer + frames). In the single-pass spine, `options` aliases `context.printState`, so that reset swapped the live writer MID-RENDER — leaf text went to the writer captured at container-enter but the block header (`writeHeader(options)`) went to the swapped writer and was DROPPED (the enclosing `.r { … }` header vanished). FIX (`serialize-helper.ts`): wrap every spine VALUE eval in `evalIsolatingSpinePrintState` (shallow snapshot + `Object.assign` restore of `printState`, sync AND async) so scratch serialization leaves the live print state byte-identical. **Verified vs less@4 reference:** the header-drop is FIXED — two async decls from two mixins now render DISTINCTLY (`.r { color: rgb(1,2,3); background: rgb(4,5,6); }`, structurally matching less@4 `#010203`/`#040506` modulo Jess's `rgb()` form-preference); async merge-across is last-wins-across-distinct-mixins byte-identical to the eval path (`.r { transform: scale(2); }`), same-body coalesce = less@4 (`rotate(90deg), scale(2)`). BEFORE/AFTER proof: with the gate removed but WITHOUT the isolation, async-merge-across renders `"transform: scale(2);\n}"` (header dropped); WITH the isolation it renders `".r {\n  transform: scale(2);\n}"`. **Perf A/B neutral** — the isolation runs ONLY inside spine value evals (`resolveSpineLeafText`/`withSpineMergePlan`); the common eval path never invokes it (zero overhead by construction), and a 400-ruleset spine-eligible A/B shows spine-with-isolation within noise of base (~15–16ms median both). Clean full rebuild: core 3280/0, `emit-walk-ratchet` 54/54 (the async-merge RESIDUAL assertion FLIPPED to a byte-identical FOLD assertion), jess `spine-production-ratchet` 86/86, all-less 105/0, error 92/0. Clears the async-dedup IOU.
- **property-MERGE value-loss + spine/eval agreement + comma-merge fold (FIXED):** three merge-subsystem correctness bugs, LAST-occurrence anchoring kept (no less@4 first-anchor expected output adopted). (1) **merge-across-mixin value LOSS** — `.m(){transform+:a} .r{.m(); transform+:b; transform+:c}` dropped the mixin's `a` (rendered `b, c`). ROOT CAUSE: `_coalesceMergedDeclarations` skipped cross-scope compose across the mixin-output boundary (`crossesMixinOutputBoundary` guard), so the mixin contribution was suppressed not combined; the same-owner successor then re-read only a truncated prefix. FIX: compose whenever a prior merged anchor accumulated a value the current value does NOT already carry as a prefix (drops the boundary guard; `composeMergedValue` is a no-op when the prefix already matches → byte-identical on the common in-scope path), and unify comma/space overlap-dedup (longest prior-suffix == next-prefix) so no item is dropped OR duplicated. Now `rotate(90deg), skew(30deg), scale(2, 4)` (comma) / space form combine ALL contributions = less@4. (2) **spine `$ref`-to-merged read** — `foo: $transform` read only the last merge sibling's own (truncated) value (`b`), not the coalesced `a b`. ROOT CAUSE: space-merge folds on the spine where the `$prop` Reference resolved mid-emit against the raw last decl, bypassing the merge plan. FIX: mirror the body merge plan onto `context.spineMergePlan` (installed/restored by `withSpineMergePlan` in lockstep with the print-options plan); `finalizeDeclarationReferenceResult` returns the anchor's combined value when the resolved decl is a plan anchor — **zero-cost fast-bail** on the undefined check when the body has no merge decl. (3) **comma-merge `+,:` did not fold on the spine** — the raw comma assign `+,:` was absent from `emit-walk` `MERGE_ASSIGNS` (so a comma body was not spine-simple → routed to eval) and from `spine-merge` `COMMA_MERGE` (recognizer). FIX: add `+,:` to both. Now comma folds like space (`deriveCalls === 0`, byte-identical to eval). Also: `normalizeMergedLeadingPlaceholder` (eval path) gained space-`Sequence` parity with the comma-`List` branch. **Perf A/B: merge-heavy (400 rulesets, comma-`+:` + `$ref`) spine ~19ms vs eval-fallback baseline ~4210ms (~200× — comma now folds instead of eval two-walking every body); common non-merge 400-ruleset workload ~38ms both branches (within noise — the `context.spineMergePlan` fast-bail is zero-cost).** Clean full rebuild post-rebase: core 3280/0, jess `spine-production-ratchet` 89 (86→89, +3 merge tests), all-less 105/0, error 92/0. Tests: jess `ruleset-merge-regression` (mixin combine comma+space, `$ref` coalesced comma+space), `spine-production-ratchet` (comma/space fold `deriveCalls===0`, `$ref`-on-spine); updated one core `reference.test` merge-chain expectation from the old value-loss (`blue`) to the coalesced (`red;blue`). This CLOSES the gate-3 "decl-ref-to-merged" residual (b) for the CONTAINER + spine `$ref` path; the root-direct gate-3 sub-shapes (a)/(c) remain eval (unreachable from Less at root).
- **recursive-arg mixin — FLAT recursion (FOLDED — gate 7):** a self / mutual name-cycle mixin with a FRAME-DEPENDENT arg (`.loop(@n) when (@n>0){ level:@n; .loop(@n-1) }` called `.loop(3)`) now FOLDS on the spine (`deriveCalls===0`), byte-identical to eval / less@4 (`level:3; level:2; level:1;`). ROOT CAUSE (the §7 gap): the re-entrant splice threads each level's freshly-bound param frame through the recursive call's ARG-binding eval (`runSpineMixinExpansion` pushes `entry.spineFrame`) — but the spliced body decls' DEDUP-KEY resolution (`runDedupPass`→`computeDeclKey`→`resolveSpineLeafText`) did NOT push the entry's `spineFrame`, so a repeated property (`level` appears N times → dedup fires) re-evaluated `@n` against the ambient caller context → `'n' is not defined`. FIX (`serialize-helper.ts`): `computeDeclKey(node, spineFrame?)` pushes `context.rulesContext = spineFrame` around the key resolution, exactly as `processNode` does for the real emit — so each level's `@n` reads its own param frame. `treeHasRecursiveMixinCall` (`emit-walk.ts`) NARROWED from "any name-cycle" to "cycle WITH a nested container" — FLAT recursion no longer gates. **Perf A/B: FLAT recursive fixture spine ~2.4ms vs eval ~4.4ms (~1.8× faster); common non-recursive mixin ~1.5ms both, benchmark.less ~273ms both — the frame push is zero-cost on the common path (only fires inside `runDedupPass` when a folded decl carries a `spineFrame` AND a property repeats).** Clean full rebuild: core 3281/0, jess `spine-production-ratchet` 90/90 (89→90; the recursion residual test → FLAT FOLD + STRIPE residual), all-less 105/0, error 92/0. STRIPE nested-container recursion now FOLDS too — see next entry.
- **recursive mixin — STRIPE nested-container (FOLDED — distinct-per-level surfaces):** a recursive cycle whose body has a NESTED CONTAINER shared across levels (`.stripe(@n){ a{…} .stripe(@n-1) }`) now FOLDS on the spine (`deriveCalls===0`), byte-identical to eval / less@4 (self / mutual / ≥3 levels / root-scope / interpolated-selector). ROOT CAUSE: the re-entrant splice re-used the SAME canonical container child per level, and the header-merge keys on node IDENTITY → two levels' `.wrap a{…}` blocks collapsed into one (`.wrap a{ border-width:2; border-width:1 }`). This also mis-folded a NON-recursive container mixin called twice. FIX (`serialize-helper.ts` `runSpineMixinExpansion`): `distinctFoldChild` splices a DISTINCT per-level copy (`copyWithReusableLeaves`, reusing scalar leaves) on a container child's 2nd+ occurrence within one expansion pass — mirroring the loop fold's per-iteration `copyWithReusableLeaves`; the first occurrence stays SHARED (zero cost on the common single-call container mixin), leaves never copied. `treeHasRecursiveMixinCall` (`emit-walk.ts`, ~95 lines) DELETED — the recursion gate is lifted entirely; an unfoldable recursive sub-shape still falls back per-call (`isSpineSimpleMixinSurface`→`anyRejected`→kind:'eval'). ROOT-scope recursion additionally needed `checkValidNodes` (`check-valid-nodes.ts`) to skip `Call`/`Mixin` nodes: the root-fold checks a recursive body's RAW surface, which still holds the unexpanded recursive `Call` (no `F_ALLOW_ROOT`, since eval expands calls before that check runs → a no-op on the eval path). **Perf A/B (same-worktree revert toggle, warmup + N median): STRIPE fixture folds faster than eval; benchmark.less neutral (stays on eval either way — the gate change is zero-cost off the fold).** Suites: core 3300/0, jess `spine-production-ratchet` 126/0 (STRIPE residual test → FOLD lock + ≥3-level/mutual coverage), all-less 105/0, error 92/0, less-parser 508/0. RESIDUAL (orthogonal, PRE-EXISTING — NOT this shape): string interpolation inside a container-body value (`url("@{base}/@{i}.svg")`) is not resolved on the mixin-container fold (affects a NON-recursive container fold equally); benchmark's `.generate-icons` exercises it but the full benchmark still routes to EVAL (other gates), so no regression. IOU: spine string-interp-in-container-value resolution.
- **@media-scoped extend gather — CROSS-MEDIA `all`-propagation (FOLDED):** `extend-media.less` now FULLY FOLDS on the spine (`deriveCalls===0`, expanded mode `collapseNesting:false`, byte-identical to the ratified `extend-media.css` + semantically to less@4). An `:extend(.ext1 all)` declared inside `@media (tv)` (`.tv-lowres`) / a nested `@media (hires)` (`.tv-hires`) / at root (`.all`) is now gathered and SCOPED: `:is(.ext1, .all) .ext2` (root), `:is(.ext1, .tv-lowres, .all) .ext3` (tv), `:is(.ext1, .tv-lowres, .tv-hires, .all) .ext4` (tv+hires). MECHANISM: the wire gather (`wireSpineExtends`, `descendChildren`) now DESCENDS into conditional-group at-rules (`@media`/`@supports`/`@container` — `isMediaScopeAtRule`), tracking a per-body scope CHAIN (the at-rule node identities, outermost-first) and tagging each gathered subject + instruction with a snapshot. The pipeline's scope filter (`pipeline.ts` `scopeReaches`) changed from exact-key-equality to a nesting-PREFIX relation: an instruction reaches a subject iff its scope chain is a prefix of the subject's — reproducing the eval reference's extend-root §A5 (`@media` body is its own root) + §A2 (descendant roots) + A3 (inner cannot reach OUT). The gate (`isSpineExtendTopology`) reject at the (formerly unconditional) at-rule-extend clause is RELAXED to media-scope at-rules; the eligibility walk (`emit-walk.ts` `isSpineEligibleAtRule`/`isSpineEligibleContainer`) threads `allowExtend` into the body check for those at-rules. `@layer`/`@scope` STILL reject (their §A4 same-layer-name mutual visibility is not a prefix relation). **Perf A/B: extend-heavy(60,4) spine 44.1ms vs eval 79.8ms — 1.81× faster, byte-equal; the scope array + prefix-compare allocate ONLY inside a media body with extends (flat documents stay on the string-key fast path — zero cost).** Clean full rebuild: core 3281/0, jess `spine-production-ratchet` 100/100 (98→100: +expanded-mode `extend.less` fold lock — the `5c6875538` fold landed WITHOUT a `deriveCalls===0` ratchet — + `extend-media` fold lock), all-less 105/0, all-less-error 92/0. Updated the stale `compiler-reuse` "public render APIs drive root eval" test (its `@media`-scoped extend now folds; switched to a `@layer`-scoped extend, which still eval-routes by design). Clears the at-rule/@media-scoped extend hard-tail residual.
- **Ruleset-level `when` guard (FOLDED):** a `when`-guarded RULESET (`.x when (…) {}`, bare-`&` `& when (…) {}`, and NESTED guarded rulesets) now FOLDS on the spine (`deriveCalls===0`), byte-identical to eval / less@4. `serializeSpineFrameContainer` evaluates the guard at DESCENT against the ENCLOSING live frame — exactly as `Ruleset.evalNode`'s definition-time guard eval (`Condition.evaluateBoolean` / `resultPasses`): a FAILING guard emits nothing (eval returns `Nil`), a PASSING one descends the body. Evaluated before the node's own scope-frame / `rulesContext` swap so guard free-vars resolve against the enclosing scope; the source `node.guard` is NOT mutated (unlike eval's COW-derive). `isSpineEligibleContainer` (`emit-walk.ts`) now admits a guarded ruleset except a not-yet-materialized STRING guard (stays eval — that path materializes first). **DIAGNOSIS CORRECTION:** MIXIN `when` guards + `default()` already folded (INCREMENT 7/8, verified) — the ONLY genuine guard gap was the ruleset-level `when`. Of the 4 measured fixtures only `namespacing-7` was gated by the guard; `mixins-guards`/`mixins-guards-default-func`/`namespacing-4` first-reject on `treeHasUnfoldableContainerBodyMixin` (NESTED MIXIN DEFINITIONS: `.lock-mixin→.inner-locked-mixin`, `.s1→.m`, `#library.core→.colors`) — NOT a guard shape (the 6b nested-def-registration residual, separate P4 item). **Perf A/B: common non-guarded 200-nested-ruleset workload spine ~15.1ms vs base ~15.3ms (neutral — the guard check fast-bails when `node.guard` unset); guarded `namespacing-7` spine ~0.50ms vs eval ~0.59ms (faster — folds instead of the two-walk).** Clean full rebuild: core 3281/0, jess `spine-production-ratchet` 100→102 (+`namespacing-7` fold lock + a ruleset-guard PASS/FAIL/nested fold lock, both `deriveCalls===0`), all-less 105/0 (only pre-existing `import-module` env red — `@less/test-import-module` missing from the worktree less.js checkout, reproduces on base), all-less-error 92/0, less-parser 491/0. RESIDUAL (the OTHER 3 fixtures, reference-verified byte-identical, eval-routed): nested-mixin-definition registration through the fold (6b) — SEPARATE from guard eval.
- **Nested mixin DEFINITION — static-namespace-path shape (FOLDED — keystone 6b):** a Mixin DEFINITION nested inside a mixin/container body now FOLDS on the spine (`deriveCalls===0`, byte-identical to eval / less@4) WHEN every path that reaches it is a STATIC authored-namespace path (`#library.core.colors()` / `#ns.mixin[@height]`). `Rules.findMixinPath` resolves such a path by walking authored container scopes (the gate-12 fallback drain) with NO dynamic per-scope registration, so the call resolves during the fold — no extra wiring needed; `treeHasUnfoldableContainerBodyMixin` was purely over-conservative for it. `treeHasUnfoldableContainerBodyMixin` now delegates the nested-def branch to a new `nestedDefNeedsDynamicRegistration(root)` (emit-walk.ts): it defers the tree ONLY when a nested-def name is reached by (a) a BARE single-segment call (leaky per-scope registration), (b) a path whose head is NOT a statically-authored top-level container, or (c) an INTERPOLATED-selector-created namespace. A NAMELESS nested Mixin (a `@map: {…}` detached-ruleset map-lookup value) is NOT a callable and no longer forces eval. **DIAGNOSIS CORRECTION (vs the prior 6b residual note):** of the 5 measured fixtures, only `namespacing-3`/`namespacing-4` were gated PURELY by the nested-def branch — both now FOLD byte-correct. The other 3 have STACKED reject beyond 6b: `mixins-guards` first-rejects on a root-level namespace-path CALL (`#ns > .guarded-mixin-for-root()` — `isSpineEligibleContainer`, a separate feature); `mixins-guards-default-func` needs LEAKY-in-container `default()`-overload dynamic registration (folds WRONG if force-lifted — unsafe); `mixins-interpolated` needs interpolated-name dynamic registration (`.Person`→`.@{n}`→`.person.sayGender()` emits EMPTY if lifted). All 3 stay eval-routed, reference-verified byte-identical, ratchet-locked. **Perf A/B (clean rebuild, file-based, warmed): base vs mine within noise on every path — benchmark.less 276.2→274.8ms, common css-3 neutral, namespacing-4 1.26→1.28ms, namespacing-3 1.47→1.50ms, mixins-guards(residual) 15.2→14.4ms. Zero-cost on the common path by construction — `nestedDefNeedsDynamicRegistration` runs ONLY once a nested Mixin-in-Mixin def is found (and only under `treeHasMixinCall`).** Clean full rebuild: core 3281/0, jess `spine-production-ratchet` 102→105 (+namespacing-4 fold lock, +namespacing-3 fold lock, +a leaky-bare-name RESIDUAL lock — all `deriveCalls` asserted), all-less 104/1 (only pre-existing `import-module` env red — `@less/test-import-module` missing from the worktree less.js checkout, reproduces IDENTICALLY on base), all-less-error 92/0, less-parser 491/0. RESIDUAL / IOU (P4): spine callable-REGISTRATION for dynamically-created nested defs (leaky bare-name + interpolated-name) — the harder half of 6b; namespace-path-reachable defs are now covered.
- **ATSTMT — bodyless CSS `@import` + `@layer` STATEMENT (FOLDED, byte-identical):** `at-rules-keyword-comments` + `layer` fixtures now FULLY FOLD (`deriveCalls===0`, spine, byte-identical to eval + less@4). Two first-rejects, one root cause each: (1) a multi-item CSS `@import` prelude (path + media/supports/`layer(...)`/comment tail) was flattened to a RAW STRING in the less-parser builder, so `isSpineFoldableCssImportStatement`'s `prelude instanceof Node && F_STATIC` always failed (a string is never a Node). FIX: when every prelude item is static, the builder wraps the joined text in an `Any` (F_STATIC) — byte-identical re-serialize, no-op eval; a non-static (interpolated) part keeps the raw string → defers to eval. The folded CSS `@import` statement HOISTS to the top-of-doc emitter via `queueTopImport` (eval-path parity, `rules.ts` `_emitRulesBody`), prepending in document order rather than at its authored source position. (2) a bodyless `@layer name, name;`/`@layer name;` layer-order STATEMENT (`AtRuleStatement`, no body) had no spine leaf → new `isSpineFoldableStatementAtRule` admits a static-prelude bodyless statement at-rule (excludes `@import`, which hoists) → emitted INLINE at position. No stacked residual: the interpolated `@layer @layer-name { … }` block form already folds via the container path. **Perf A/B (clean rebuild, warmed, benchmark.less): base 333.2ms vs mine 323.9ms — within noise, no regression; zero-cost fast-bail (type-check gated, only fires on `AtRuleStatement`).** Clean full rebuild: core 3281/0, jess `spine-production-ratchet` 115→117 (+ATSTMT-1 CSS-import-statement fold lock, +ATSTMT-2 whole-`layer`-file fold lock — both `deriveCalls===0` asserted), all-less 105/0, all-less-error 92/0. Landed `origin/dev` (`a2bb295f8`).
- **`@page` MARGIN-BOX at-rules + at-rule-prelude VARIABLE resolution (FOLDED, byte-identical):** the 16 CSS Paged-Media §5 margin-box at-rules (`@top-left`/`@top-center`/`@bottom-right-corner`/…) as `@page` body children now FOLD (`deriveCalls===0`, spine, byte-identical to eval + less@4). First-reject was `isSpineEligibleAtRule` (margin-box NAME in no eligible set → whole `@page` root dropped to eval). FIX (`emit-walk.ts`): a `SPINE_PAGE_MARGIN_BOX_AT_RULES` set admitted via the declaration-bodied root-only body path (a margin-box is `isNestable()`/`isRootOnly()`-false → emits in place, structurally the wrap+emit shape one level down; the spine emit dispatch already renders any `AtRule`-with-`Rules` child this way). SURFACED + FIXED a co-resident latent spine bug (media.less has NO `@charset`, so the margin-box reject was the ONLY thing keeping it on eval — folding it flipped media.less onto the spine): a bare `@var` in an at-rule prelude (`@media (max-width: @bpMedium)`) emitted the RAW unresolved index-ref (`$[bpMedium]`) and split the hoist-merged block, because when a nested at-rule HOISTS the frame-diff loop re-materializes the ANCESTOR at-rule's header (`writeHeader`/`getHeaderString`/`getComparableHeaderString`) at a point where the single `atRuleHeaderNode` render-local override no longer targets it. FIX (`print.ts` + `serialize-helper.ts` + `at-rule.ts`): a render-local `spineResolvedPreludes: Map<AtRule, prelude>` (node-keyed, save/restored per descent — NOT stored on the shared canonical node, per the `spineSelector` invariant) that `serializeSpineFrameAtRule` registers and those three header paths consult; generalizes to `@supports`/`@container` var preludes. **Perf A/B (synthetic spine-heavy render): base(dev) 39.2ms vs folded 38.7ms — within noise, no regression; two `Set.has` fast-bails reached only inside a root-only at-rule body.** Clean full rebuild: core 3277/0 (clean-base baseline), jess `spine-production-ratchet` 121→122 (+`@page` margin-box `deriveCalls===0` fold lock), all-less 105/0 (media.less folds byte-identical), all-less-error 92/0. Landed `origin/dev`.
- **IOU (LOW-PRIORITY, NOT a flip blocker):** mixin-surface param-frame reuse across an ALIASED shared body — a `.mixin(@x)` def whose body nests an at-rule inside another at-rule (`@media handheld { @media (max-width: @x) {…} }`), called WITH an arg then with NO arg on the SAME node instance, leaks the first call's `@x` frame into the second (default not applied). Reachable ONLY via a non-parser hand-built/aliased AST — real parsing creates DISTINCT per-call surfaces (verified: parsed source renders `.a`=100px, `.b`=200px correctly; the `at-rule.test.ts` "serialize the exact AST structure" fixture was de-aliased by splicing a real parse of the `.mediaMixin` region rather than hand-inlining one shared body). Lives in the callable mixin-surface machinery (`callable-param-match.ts`/`callable-candidate-match.ts`/`rules.ts`), outside `serializeSpineFrameAtRule`.
- **Docs (unblock producer work):** STRINGS-OVER-NODES, P4-ENDGAME-PLAN, conditional-decls, extend-4a, any-name-reduction, basic-selector-boundary, parseman-trivia-audit.

## 🔵 In-flight
- _(none)_

### STMTCALL (bare statement-position function call) — LANDED (nested void + value fold; DR-call + function-value-decl registry-reachability head FOLDED → `functions.less` FULLY FOLDS)
- **Shape — a bare STATEMENT-POSITION built-in FUNCTION call** (`if((false), {g: 7});` in `functions.less #if`; `e('…');` in `css-escapes.less`): a `Call` whose name is a `function`-type `Reference` (NOT a mixin / mixin-ruleset / `variable` DR-call — those already fold via `isSpineEligibleMixinCall`). Post condition-grammar change `if` is an ordinary `Call` (the `IfCall` special node is gone), confirmed empirically: `functions`'s first-reject is `Call nameType=Reference/type=function/key="if" args=2`. `isSimpleSpineLeaf` rejected ANY non-declaration/comment leaf → forced `#if` (and the whole `functions` root) onto eval.
- **FOLD** (`isSpineFoldableStatementCall` gate + `resolveSpineStatementCallText` emit, emit-walk.ts / serialize-helper.ts): a NESTED statement call now evaluates inline against the live frame and serializes its result exactly as the eval call-lane does — a VOID result (`if`-false-no-else → an empty `Anonymous`) emits NOTHING (no blank line, no stray `;`, surrounding `/* void */` trivia preserved); a VALUE result (`e()` unquote) emits its text as its own line with the source `;` DROPPED. Byte-identical to eval AND less@4 (which errors on the non-void statement-`if` forms — `DetachedRuleset node returned by a function is not valid here` — so only the void shape is a real feature; the fold reproduces eval for both without bailing). Zero-cost off the shape (Call-type + reference-type bail).
- **ROOT-DIRECT statement call — FOLDED (`isSpineEligibleRoot` exclusion DELETED; `css-escapes.less` FULLY FOLDS).** A root-direct `e('/* anything to unquote */');` (css-escapes trailing statement) used to route the whole tree to eval: the root emitter `Rules._emitRulesBody` appended the source `requiredSemi` `;`. FIX: the root leaf tail grew the SAME resolve-inline-and-drop-`;` branch the CONTAINER leaf tail uses — `resolveSpineStatementCallNode` evaluates the `Call` against the live frame; the resolved node is run through `checkValidNodes` at ROOT (`F_ALLOW_ROOT`) so a value-returning call resolving to a non-statement node (`rgba(0,0,0,0);` → `Color`) throws `eval/invalid-statement` exactly as the eval terminal did (a legal `Anonymous`/comment from `e('…')` passes), then serialized with the `;` dropped (void → suppressed). `resolveSpineStatementCallText` refactored into `resolveSpineStatementCallNode` + `serializeSpineStatementCallNode` (container path unchanged). The escaped-selector NESTED mixin call `.mixin\!tUp()` inside `.\34 04 strong` already folded (broad mixin fold) — so with this the whole `css-escapes.less` renders in ONE spine pass (`deriveCalls===0`, byte-identical to the eval reference + expected `.css`). **Perf A/B (benchmark.less, 40-iter median, 10 warm, clean rebuild): branch 286/292ms vs base 292/300ms across two rounds — neutral-to-faster, no regression (removed a gate loop, added a fast-bailing `isSpineFoldableStatementCall` branch; zero-cost off the shape).** Gate (clean rebuild): core **3277/0**; jess `spine-production-ratchet` **124→125** (+`css-escapes.less` whole-fixture `deriveCalls===0` lock); all-less **105/0**; all-less-error **92/0** (the `rgba(...)` root-value error `functions-5-color-2` still errors — preserved).
- **DR-call + function-value-decl head — FOLDED (registry-reachability fix; `treeHasDetachedCallWithFunctionValueDecl` guard + `isDetachedRulesetCall`/`isFunctionCall` helpers DELETED).** After the statement-call fold, `functions` revealed a PRE-EXISTING spine gap unrelated to statement calls: a tree that folds a DETACHED-RULESET call (`@1();`) AND has a declaration whose value contains a built-in FUNCTION call broke downstream function-`Call` resolution — the `function`-type Reference resolved to a raw `Any` instead of its registered `JsFunction`, emitting raw call syntax (`length(a 1, b 2)` unresolved). Reproduced minimally: `#i { @r:{c:3}; @r(); } #l { @list:a 1,b 2; length: length(@list); }` folded to raw `length(...)`, though each block folded correctly ALONE, and — decisively — the SAME tree with the `@r()` call REMOVED folded correctly. **ROOT CAUSE:** on the spine there is no `Rules.eval` frame for the real root on `context.rulesEvalStack`, so a detached-ruleset body evaluated inside the fold reached `Rules._evalPreparedRules` (rules.ts) as the FIRST `Rules.evalNode` — its `rulesEvalStack.length === 1` "am I the outermost root?" heuristic fired and REASSIGNED `context.root` to that nested DR body (which carries no function registry). `Rules.findFunction`'s dead-end fallback reads `context.root.functionsByName`, so the subsequent `length` lookup missed → raw output. The eval path never trips this: it pushes the real root first, so a nested body sees stack length ≥2 and the reassignment is a no-op. `context.treeRoot` (still the real root) confirmed the discriminator: broken lookup saw `root.fn=n/a treeRoot.fn=91`. **FIX** (`Context.spineOwnsRoot`): `renderRootViaSpine` sets `spineOwnsRoot = true` (scoped save/restore alongside the existing `context.root` save/restore); `_evalPreparedRules` skips the outermost-root reassignment when it is set. The registry stays reachable and the function computes. Also fixes the narrower MIXIN-body head (`replace('foo-1', …)` on a folded mixin surface — same reassignment). Undefined on the eval path (zero-cost, unchanged there). `functions.less` now FULLY FOLDS byte-identically (`deriveCalls===0`, single spine pass); no further head remains.
- **Gate (clean full rebuild):** core **3281/0**; jess `spine-production-ratchet` **120→121** (flipped the DR-call + function-value-decl residual-lock negative ratchet → a byte-correct `deriveCalls===0` FOLD test, + a new whole-fixture `functions.less` fold lock); all-less **105/0** (includes `functions.less` byte-identity); all-less-error **92/0**. **Perf A/B** (benchmark.less, 15-iter median, 3 warm): baseline (fix reverted) **341.6ms** vs fixed **331.7ms** — no regression (within run-to-run noise / marginally faster; the fix is a single boolean set at spine entry + one boolean check per Rules eval).

## ⏸ Queued P4 folds → 100% spine coverage (core-lane, one at a time)
- ~~nested-container mixin body~~ (simple + at-rule-through-hoist 6a landed; static-namespace-path nested DEFINITION 6b landed — see ✅. IOU remaining: spine callable-REGISTRATION for dynamically-created nested defs — leaky bare-name (`.lock-mixin→.inner-locked-mixin`, `default()`-overload in-container) + interpolated-name (`.Person→.@{n}→.person.sayGender()`))
- ~~merge-across-mixin~~ (static + ~~async-valued (gate 8)~~ landed — see ✅) / ~~mixin-as-value / detached-ruleset arg~~ (landed — see ✅)
- import edge-modes: ~~interpolated-path retry~~ (case A + case B landed — see ✅; `(inline)`-in-deferred-body residual gated, IOU to eliminate at P4), ~~extend-through-import~~ (plain transitive landed — see ✅), compose/forward
- ~~reference-mode~~ (extend-into-reference landed — see ✅; rich-reference-body residual gated, IOU to eliminate at P4); value frontier; ~~at-rule-`&`-through-hoist~~ (landed — see ✅; ~~mixin-surface analogue~~ landed too — see 6a ✅)
- **measured-last (A/B, revert-churn risk):** ~~namespace-path over imported namespace~~ (gate-12 landed — see ✅; the "namespace-merge" framing was a MISATTRIBUTION — it was a fallback-frame drain gap, not a same-named merge), ~~recursive-arg mixin~~ (FLAT recursion gate-7 landed AND STRIPE nested-container recursion landed via distinct-per-level surfaces — see ✅; recursion gate fully lifted)
- **extend hard-tail (poor-ROI-last):** (A) multi-branch selector-list subjects + nested-ruleset-subject targets → fully flips `extend.less`; ~~(B) extend-in-@media (at-rule-scoped gather)~~ (LANDED — see the @media-scoped extend gather entry ✅)
- inner-fallback elimination (mixin/import `kind:'eval'` candidates)

## 🔒 Gated / deferred (not blocked-on-me)
- **Producer flips** (jess-parser emits strings: BasicSelector-position, Any static tokens) — gated on parseman trivia compiler change. Keystone STRINGS-OVER-NODES + never-revert guardrail in place.
- **parseman trivia compiler change** (Option A fuse-time default vs B caller-context) — **owner's other agent**.
- `:=` spine fold (mechanism-B); cross-scope `setDefined`; node-field slimming.

## 🧩 Feature track (parallel to the cutover): Assignable control nodes
See `ASSIGNABLE-CONTROL-NODES-PLAN.md` — make mixins/`each`/`$for`/`$if` value-returning (arrow-function shape).
Sequencing (rationale: control nodes have TWO evaluators — eval + spine — until the D-EVAL flip; the plan's #1 risk is that divergence):
- ⏸ **Phase 0** (`each(list, @($item) > *[…])` → SelectorList; jess PR #88): NEAR-TERM. Value-position → routed OFF the spine by construction, so it dodges the divergence risk pre-flip. Touches hot `control.ts`/`interpolated.ts` — land in a quiet window; bump ahead only if PR #88 is blocking. Effort medium / risk medium.
- 🔒 **Phases 1–3** (general `result:` function-return primitive → generalize value-mode `each`/`$for`/`while` + `$if`-value → Less back-port `#(params) > <expr>`): AFTER the D-EVAL flip — single path post-flip means value-mode is implemented ONCE with no eval/spine sync. Do NOT build these pre-flip.

## 🎯 The deletions — D-EVAL flip (ALL-OR-NOTHING, gated on 100% coverage; one coordinated flip)
Deletes together: eval two-walk (`evalForRender`/`Rules.derive`/`_deriveShell`) + output-tree staging + clone families (D1) + `F_STATIC`/`F_NON_STATIC`/`F_HAS_NODE_CHILD`/`F_CHILD_DERIVED` + `propagateFlagsFrom` (D2) + TreeVisitor/preSerializeRoot (D3) + **old extend apply** `extend-roots`/`processExtends`/legacy-`extend.ts` (D4) + `inherit` span (D5). `treeContext` (D6) is NOT independently deletable (it's the per-node provenance carrier — verified) → also rides the flip.

### D-EVAL flip MUST carry — root-level `isSpineEligibleRoot` gates 1a/1b (document-framing, NOT a fold)
Investigation verdict (three whole-root eval-abort gates in `emit-walk.ts` `isSpineEligibleRoot`):

- **Gate 1a — `context.currentCharset` → (ii) flip-time framing, NOT a fold.** `currentCharset` is populated ONLY by `Rules._prepareRegistration` (rules.ts:5736), which converts a root `@charset` (`Any` role='charset') into a `Nil` placeholder. On a FRESH render the spine-eligibility check runs BEFORE prep, so `currentCharset` is `undefined` and the gate never fires; the charset node ITSELF (an `Any` pre-prep, a `Nil` post-prep) is rejected at the body-leaf level (`isSimpleSpineLeaf` returns false for both) — proven: with `currentCharset` force-cleared, a prepared charset root is STILL ineligible. The gate is load-bearing ONLY on a REUSED context (a re-render on the same `Context`, where `currentCharset` persists): rendering a SEPARATE charset-free spine-eligible tree on that context must re-emit the persisted `@charset` document prelude — the eval path does this (`evalForRender` rules.ts:4950 routes a `currentCharset`-carrying root to the passthrough state, `_toDocumentString` rules.ts:4280 emits the charset first). `renderRootViaSpine` DELIBERATELY does NOT emit charset (emit-walk.ts:2224 "Charset is already gated OUT of the spine") and only prepends imports IT queued this descent. Production always builds a FRESH `Context` per compile (jess/src/index.ts:926/952) → this gate is unreachable in production; it guards the reused-context re-render API. **At D-EVAL flip:** the spine's own document-framing path must emit the persisted-`currentCharset` prelude at depth 0 (mirror `_toDocumentString`'s charset-first emit), or the flip must guarantee per-render context reset of `currentCharset`/`charsetEmitted`. Then delete the gate.

- **Gate 1b — `!allowImport && context.topImports?.length` → (ii) flip-time framing, NOT a fold.** Structural twin of 1a for `@import`. `topImports` is populated during eval/wire (`queueTopImport`) and is NOT globally cleared after a render (only trimmed to `topImportsBaseline` on a spine ABORT). On a reused context a prior render's `topImports` persists; the CURRENT tree has NO import nodes (`engageImportLayer` false → `allowImport` false, so nothing rejects at body level), so this gate is the ONLY guard. Proven: with a stale `topImports` injected, the eval path re-emits `@import "foo.css";\n.b {…}` (the persisted prelude via `_toDocumentString` rules.ts:4287) whereas the spine (`renderRootViaSpine` → `renderQueuedTopImports`) prepends ONLY what IT queued this descent → would DROP the stale prelude. Same production note as 1a (fresh `Context` per compile → unreachable in production). **At D-EVAL flip:** same requirement as 1a — the spine document-framing path must prepend a persisted-`topImports` prelude, or the flip guarantees per-render reset of `topImports`. Then delete the gate.

### gate 9 (leaky mixin-body-var forward-propagation) — LANDED (folded; gate + detector DELETED)
- **Gate 9 — `leakyScope && treeHasMixinCall && treeHasLeakyConsumedMixinBodyVar` → FOLDED. Gate + `treeHasLeakyConsumedMixinBodyVar` detector DELETED.** In leaky Less mode a mixin body's plain `@x: v` VarDeclaration LEAKS into the CALLER scope; a same-scope consumer (`.a { .mix(); width: @x }`, an EARLIER `width: @x` sibling — Less resolves a scope's vars lazily last-wins, NOT source-order gated, verified against less@4 — a call-arg `@x`, a nested-container child, or a later root SIBLING ruleset after a root-level call) reads it. The spine now propagates the leak into the caller's live `ScopeFrame`, byte-identical to less@4:
  - **Container-body leak** (`runSpineMixinExpansion`, serialize-helper.ts): at the surface splice, `Rules.injectSpineLeakyMixinSurfaceBindings(surface, callIndex, ctx)` registers each folded surface's plain (non-`setDefined`) `@x` into the CALLER frame (the enclosing container, or the surface for a nested call). The leaked value is bound through a cell whose `prepareValue` resolves the decl against the SURFACE frame, so a param-dependent leak (`@x: @a`) reads the bound param — a shape jess EVAL itself throws on (`'a' is not defined`), so the fold is MORE correct than eval here and matches less@4 (`width: 20px`). The gate node carries `index = callIndex` via a prototype-delegating marker so the shared surface decl's OWN `index` (load-bearing for a body-internal `snapshot` read) is NOT mutated.
  - **Root-level leak** (`evaluateCallableCandidateOutput`, callable-candidate-output.ts): a root-level mixin call folds via the eval terminal (the root emit path installs no surface sink), so after `attachMixinOutputSlot` the evaluated output's leaked vars inject into the SOURCE-root frame (captured as `context.spineRootCallEmitFrame` at the root emit site, since `context.root` is reassigned during the nested eval) via the existing `Rules.injectLeakyMixinOutputBindings`.
  The injection is scoped to the caller frame (a later OUT-of-scope sibling still sees the outer binding — verified byte-identical to less@4). Zero-cost off leaky mode or when a folded surface has no plain var. **Dedicated FOLD ratchet:** 7 tests in `packages/jess/test/spine-production-ratchet.test.ts` (container-body, earlier-sibling lazy scope, param-dependent, root-level cross-ruleset, scope isolation, `Rules.derive` uncalled, non-leaky strict). Gate numbers: core 3281/0, ratchet 90→97, all-less 105/0, all-less-error 92/0; benchmark.less A/B within run-to-run noise, leaky-consume folds ~faster than eval (no output tree). NOTE: leaky mode IS default for Less input (jess-plugin-less defaults `leakyScope: true`).

### gate 12 (namespace-path over an imported namespace) — LANDED (folded; gate + `treeHasNamespacePathCall`/`treeHasStyleImport` detectors DELETED)
- **Gate 12 — `allowImport && treeHasStyleImport(root) && treeHasNamespacePathCall(root)` → FOLDED. Gate + both detectors DELETED.** MISATTRIBUTION CORRECTED: the old comment blamed a "same-named merge" the fold couldn't model. It is NOT a merge problem. `namespacing-2` has a LOCAL `#library { .sizes() }` (overriding, → 800px) and consumes `#library.add-one(1px)[@return]` — a member that ONLY the imported `#library` defines. The shared namespace-path lookup (`Rules.findMixinPath` → `findRulesetNamespacePathFast`) walked ONLY `this`'s primary parent chain: the LOCAL `#library` head HITS first, its remainder MISSES `.add-one`, and the walk NEVER re-tried the import `fallbackFrame` (installed by `wireSpineImports` → `linkImportFallbackFrame`, often on an ENCLOSING frame, not the leaf lookup scope). So the fold threw `No matching mixins found for '#library.add-one'` → empty output. The plain-var / string-key path already drained the fallback (`findMixin`, rules.ts); the ARRAY-key namespace path bypassed it.
- **FIX** (`Rules.drainNamespacePathFallback`, rules.ts): after the primary namespace-path walk MISSES, drain the fallback-frame chain reachable from the primary scope chain (parent-chain-first encounter order, mirroring the string-key `findMixin` drain) and re-run the SAME path against each fallback frame's `rulesNode`. A local hit NEVER reaches the drain (it returns first), so eval stays byte-identical; the drain only ADDS resolution the primary walk missed. Called at the four `findMixinPath` miss-exits (empty ruleset-fast result, local-ruleset-head defer, no-namespace-mixin, tail). **Zero-cost on the common path:** the `!primaryFrame` / no-reachable-fallback-head guard bails before allocating anything — a no-import namespace lookup pays nothing.
- **Perf A/B (spine, fixed vs origin/dev baseline, 2 runs each):** no-import common namespace lookup ~0.123ms both (medians 0.122/0.124 baseline vs 0.126/0.125 fixed — micro-op noise); benchmark.less ~59.5ms both (medians 59.1/59.8 baseline vs 59.9/59.7 fixed — within run-to-run noise). No measured regression on eval OR the common path.
- **Verified:** eval + spine byte-identical to less@4 `namespacing-2.css` (`.bar{width:800px;height:2px} .foo{width:800px} .lunch{treat:ice cream}`). Clean full rebuild: core 3281/0, jess `spine-production-ratchet` 97/97 (the increment-2 "STAYS on eval / namespace-merge wall" test → a byte-identical imported-namespace-path FOLD test), all-less 105/0, all-less-error 92/0.
</content>
