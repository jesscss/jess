# Cleanup

This file is deprecated as an active tracker.

Active tracking in `docs/future/node-copy-reduction/` should only cover:

- field-aligned edges
- `RenderKey`-selected reads
- cursor-based traversal

General cleanup work, dead-code passes, import pruning, and unrelated file
cleanup do not belong in the node-copy-reduction tracking set.
