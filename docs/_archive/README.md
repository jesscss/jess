# Archived docs (`/docs`)

This folder contains **dev-facing, legacy, or superseded** notes that used to live directly under `/docs`.

- We keep these as **dated snapshots** to reduce clutter and preserve history.
- User-facing docs live in the Docusaurus site (`packages/docs`); do not archive/remove those.
- `future-performance-2026-04-13/` contains older registry/benchmark proposal
  and audit material. It is historical context only; active runtime guidance
  lives in `docs/future/performance/2026-04-13-registry-redesign-handoff.md`,
  `docs/future/node-copy-reduction/`, and `docs/future/pre-eval-elimination.md`.
