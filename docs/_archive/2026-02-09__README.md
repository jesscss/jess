# Archived: `docs/README.md`

This was a legacy “quick and dirty” overview under `docs/` and explicitly noted it was outdated.

- **Why archived**: user-facing docs live in `packages/docs` (Docusaurus); dev-facing tracking lives in `.cursor/`.
- **Where the content went**: preserved in **git history** for `docs/README.md`.

